# google-reviews-download — Roadmap

_Last updated: 2026-05-30_ (**Phase 26 — Suite-deepening tranche, export-xlsx module-surface + per-call bytes-freshness half opened** (D-083): with L25.1 merged (#80) every standard Phase 0–25 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and per D-046/D-051 the routine opens a deepening rather than idle. D-082's closing line named `tests/export-xlsx.test.ts` as one of the next candidates (the XLSX writer's `Object.keys(module)` surface is unpinned even though L12.1/D-065 deepened the per-column/per-row semantics). L26.1 deepens that suite with three cross-cutting load-bearing concerns in the Phase 13/15/18/19/20/21/22/23/24/25/D-027/D-070/D-072/D-075/D-076/D-077/D-078/D-079/D-080/D-081/D-082 "pin the silent-regression edges of an already-tested module" pattern: (a) module's named-export surface — `Object.keys(xlsxModule).sort()` exactly `["XLSX_COLUMNS","XLSX_CONTENT_TYPE","__testing","formatReviewsAsXlsx","xlsxFilename"]` (5 runtime exports; type-only export `XlsxColumn` is erased); a surplus `export const SHEET_NAME` re-exposing the internal sheet name or an `export function buildWorkbook` extracted mid-refactor would silently broaden the public contract every importer is held to; (b) `formatReviewsAsXlsx(payload)` returns a fresh `Uint8Array` per call — two `it`s, one for the non-empty payload path and one for the empty-reviews path; a `const CACHE = new WeakMap<CachedReviewsPayload, Uint8Array>(); ... return cached;` memoise-by-payload refactor would silently share a single buffer across every call with the same payload reference, and any downstream `.fill(0)`/byte-rewrite would leak across callers — the route handler reads `bytes.byteLength` for Content-Length and passes the same buffer to `NextResponse`, so a shared singleton would also mean two concurrent requests race on the buffer; (c) `__testing` namespace's exact key surface — `Object.keys(__testing).sort()` exactly `["COLUMN_WIDTHS","PHOTO_URL_JOIN","SHEET_NAME","rowFor"]` (4 helpers, all consumed by this suite); a surplus 5th helper (e.g. a `freezePane` builder, a `widthFor(col)` lookup) leaking in would silently broaden the test-only contract. Mirrors L25.1/D-082's place-id deepening pushed onto the XLSX writer module. Suite grows from 23 it-blocks/8 describes to 27 it-blocks/11 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 25 — Suite-deepening tranche, place-id module-surface + per-call freshness half opened** (D-082): with L24.1 merged (#79) every standard Phase 0–24 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and per D-046/D-051 the routine opens a deepening rather than idle. D-081's closing line named `tests/place-id.test.ts` as one of the next candidates (the canonicalisation module's `Object.keys(module)` surface is unpinned even though L11.3/D-061 deepened the per-rule semantics). L25.1 deepens that suite with three cross-cutting load-bearing concerns in the Phase 13/15/18/19/20/21/22/23/24/D-027/D-070/D-072/D-075/D-076/D-077/D-078/D-079/D-080/D-081 "pin the silent-regression edges of an already-tested module" pattern: (a) module's named-export surface — `Object.keys(placeIdModule).sort()` exactly `["PlaceIdParseError","__testing","normalisePlaceId"]` (3 runtime exports; type-only export `NormalisedPlaceId` is erased); a surplus `export const PLACE_ID_REGEX` / `export function isValidPlaceId` would silently broaden the public contract every importer is held to; (b) `normalisePlaceId(input)` returns a fresh `{raw, slug}` object per call — two `it`s, one for ChIJ and one for MOCK_; a `const RESULTS = new Map<string, NormalisedPlaceId>(); ... return cached;` memoise-by-input refactor would silently share a single result object across every call with the same input, and any downstream `.raw`/`.slug` mutation would leak across calls — `.toEqual()` cannot catch this; (c) `__testing` namespace's exact key surface — `Object.keys(__testing).sort()` exactly `["PLACE_ID_PATTERN","canonicalisePrefix","slugify"]` (3 helpers, all consumed by this suite); a surplus 4th helper (e.g. `SHORT_LINK_HOSTS` re-export, an `isMockId` predicate) leaking in would silently broaden the test-only contract. Mirrors L23.1/D-080's sf-client + L24.1/D-081's reviews-cache deepening, pushed onto the place-id module. Suite grows from 19 it-blocks/6 describes to 23 it-blocks/9 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 24 — Suite-deepening tranche, reviews-cache module-surface + factory-freshness half opened** (D-081): with L23.1 merged (#78) every standard Phase 0–23 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and per D-046/D-051 the routine opens a deepening rather than idle. D-080's closing line named `tests/cache-key.test.ts` as the next candidate; the actual suite is `tests/reviews-cache.test.ts` (the place-id-to-cache-key normalisation lives in the reviews-cache module). L24.1 deepens that suite with three cross-cutting load-bearing concerns in the Phase 13/15/18/19/20/21/22/23/D-027/D-070/D-072/D-075/D-076/D-077/D-078/D-079/D-080 "pin the silent-regression edges of an already-tested module" pattern: (a) module's named-export surface — `Object.keys(reviewsCacheModule).sort()` exactly `["CACHE_KEY_PREFIX","CACHE_TTL_SECONDS","__testing","cacheKey","createReviewsCache"]` (5 runtime exports; type-only exports `CachedReviewsPayload`/`ReviewsCache`/`ReviewsCacheOptions` are erased); a surplus `export const CACHE_VERSION` would silently broaden the public contract every importer is held to; (b) `createReviewsCache()` returns a fresh cache instance per call on BOTH branches — two `it`s, fixture/MemoryCache and KV/KvRestCache; a `const SINGLETON = new MemoryCache(...); return SINGLETON;` cleanup would silently let request-scoped store mutation leak across callers, and the same regression target exists on the KvRest branch; (c) `__testing` namespace's exact key surface — `Object.keys(__testing).sort()` exactly `["KvRestCache","MemoryCache"]` (2 class-constructor handles); a surplus 3rd helper leaking in would silently broaden the test-only contract. Mirrors L23.1/D-080's sf-client deepening, pushed onto the cache module. Suite grows from 17 it-blocks/5 describes to 21 it-blocks/8 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 14 — Suite-deepening tranche, SEO/freshness half opened** (D-071): with L13.2 merged (#67) every standard Phase 0–13 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and per D-046/D-051 the routine opens a deepening rather than idle. D-070's closing line explicitly named `tests/seo-routes.test.ts` as the next candidate (the `lastModified` freshness contract was unpinned). L14.1 deepens that suite with three cross-cutting load-bearing concerns in the Phase 13/D-070 "two-private-copies-of-the-same-helper-must-not-drift" pattern: (a) cross-module `siteUrl()` symmetry on all four env states — `app/robots.ts` and `app/sitemap.ts` each carry a *private* copy of `siteUrl()` (same `FALLBACK_SITE_URL`, same trim+slash-strip+blank-fallback chain), the existing suite pins all four env states on the robots side but only one on the sitemap side, a drift in either copy silently makes robots' `host` directive disagree with every sitemap URL's origin (Google's canonical-host cross-reference breaks with no signal); pinned table-driven across `[unset, trailing-slash, whitespace-pad, blank]` × `robots().host === sitemap-root-origin === expected`, with robots' `sitemap` URL also pinned at `${expected}/sitemap.xml`; (b) `lastModified` freshness — `Date` instance + computed inside the call window (`before ≤ ts ≤ after` around `sitemap()`) + reference inequality across two calls (`second !== first` — a module-level `const now = new Date()` would return the same instance forever even if timestamps coincided, so the reference check is strict-stronger than timestamp equality); (c) URL uniqueness as `new Set(urls).size === urls.length` — pre-L3.1b trivially 1, but the load-bearing guard against a regression that double-spread `publishedVariants()` post-L3.1b (the existing `urls.has(...)` lookups are silent about duplicates). Suite grows from 10 it-blocks/2 describes to 17 it-blocks/5 describes; suite-file count unchanged at 19 (deepening only). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 13 deepens further** (D-070): L13.2 lands the deepening of `tests/healthcheck-route.test.ts` — the SF liveness-probe suite that already pinned ok/down status, mode-from-`SF_API_KEY`, `Cache-Control: no-store`, and the L8.5 client-injection seam, but tested only `__testing` internals (PROBE_PLACE_ID + handle seam) at the route-config layer. Four cross-cutting load-bearing concerns in the Phase 13/D-069 "public-export end-to-end" pattern: (a) `runtime = "edge"` named-export freeze in its own one-`it` describe (the route-config analogue of L13.1's `config.matcher` — a dropped export silently moves the probe to Node.js runtime); (b) response-envelope structural shape via `Object.keys(body).sort()` exact equality on ok (five keys, no error) AND down (six keys with error) AND `Object.keys(body.error).sort()` exactly `["code","message"]` — symmetric with L13.1's middleware envelope (D-027); (c) `Content-Type: application/json` pinned on both ok and down paths; (d) `latency_ms === Date.now() - startedAt` arithmetic direction + non-constant via `vi.spyOn(Date, "now").mockReturnValueOnce(start).mockReturnValueOnce(end)` with **distinct** integer deltas on ok (500) and throw (750) paths so a refactor that hard-coded `latency_ms: 500` passes ok and fails down — pinning the field is *computed*, not a constant. `new Date()` for `checked_at` uses a separate internal time source so the spy intercepts exactly the two Date.now calls in `handle()`. Suite grows from 12 it-blocks/4 describes to 19 it-blocks/8 describes; suite-file count unchanged at 19 (deepening only). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 12 deepens further still** (D-068): L12.3 lands a deepening of `tests/cn-util.test.ts` — the `cn()` className helper every component in the tree calls to compose classes. The existing 11 it-blocks/2 describes proved the clsx input-handling layer (string/array/object/conditional inputs joined, every falsy entry dropped, empty result is `""`) and the tailwind-merge same-class last-wins rule (`cn("p-2","p-4")==="p-4"`, conflict via a conditional, repeat dedupe, non-conflicting survival). Three cross-cutting load-bearing concerns were unguarded — the silent-regression edges of `cn()` are the merge behaviours components *actually depend on* but the trivial same-class case can't reach. **First: directional vs shorthand interaction.** The `cn(base, overrides)` component pattern is exactly `cn("p-4", "px-2")` — must yield "p-4 px-2" (later more-specific overrides only its axis, the broad stays for the other axes); reversed `cn("px-2", "p-4")` collapses to "p-4" (later broader nukes the specific). A regression to "naive same-prefix last-wins" would still pass every existing test (they only cover same-class `p-2`/`p-4` conflicts) and silently break every component using the override pattern. Pinned in four directions: `p-4` + `px-2` → both survive; `px-2` + `p-4` → `p-4` only; `p-4` + `pt-2` → both survive (single-side against broad axis); `pt-2` + `pl-4` + `p-8` → just `p-8` (multi-step collapse chain). **Second: variant-aware conflict scoping.** `hover:`/`sm:` variant prefixes open *independent* merge buckets, so `cn("p-2", "hover:p-4")` keeps both (different scopes) but `cn("hover:p-2", "hover:p-4")` collapses to `hover:p-4` (same scope). A regression that ignored variants would nuke every responsive/state-prefixed utility paired with an unprefixed one — and the "merge by suffix" regression is silent on the existing same-class tests. Asserted across two distinct variants (`hover:` and `sm:`) so a partial regression on one fails, plus a three-bucket case (`"p-2 sm:p-4 sm:p-8 hover:p-4"` → `"p-2 sm:p-8 hover:p-4"`) proving the bucketing is real, not coincidental. **Third: property-aware (not prefix-aware) conflicts.** `text-sm` (font-size) and `text-blue-500` (color) share the `text-` *prefix* but target distinct CSS properties — both must survive. A "merge by token prefix" regression would silently strip the type-scale class from every coloured-text element on the site. Pinned with: `text-sm` + `text-blue-500` → both survive (load-bearing keep-both); `text-red-500` + `text-blue-500` → last wins (same property, color); `font-bold` + `font-medium` → last wins (second property family so the property-awareness can't pass vacuously on color alone); `bg-blue-500` + `text-white` → both survive (the trivial wholly-different case, guards against a "drop on any shared substring" regression). Plus the zero-arg edge (`cn()` → `""`) the existing "no class survives" test missed, and the pre-joined-string edge (`cn("p-2 p-4")` → `"p-4"`, tokenising whitespace inside a single arg, not just across args). Suite grows from 11 it-blocks/2 describes to 24 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 12 deepens further** (D-067): L12.2 lands the deepening of `tests/reviews-cache.test.ts` — the cache module that decides every `/api/reviews` hit/miss path and the only KV-REST wire-format guard the repo carries. The existing 9 it-blocks/3 describes covered the versioned key shape, MemoryCache TTL expiry via an injected clock, and the KV REST pipeline body shape on the happy paths. Three cross-cutting load-bearing concerns were unguarded — the silent-regression edges of a cache module are TTL drift (silent SF-spend doubling), error-class narrowing (silent route crash on KV outage), and asymmetric wire shape between `get` and `set` (silent live-KV rejection on read). **First: TTL boundary direction + absolute `CACHE_TTL_SECONDS` freeze.** `MemoryCache.get` uses strict `>` against `expires_at`, so the tick that equals `expires_at` must still be a hit; a refactor flipping to `>=` would expire entries one ms early on every call (a fixed 1-ms TTL truncation invisible until the rare overlap of `now()` and `expires_at`). Pinned at both sides of the boundary (`t = CACHE_TTL_SECONDS * 1000` → hit; `t = CACHE_TTL_SECONDS * 1000 + 1` → first miss). Paired with `expect(CACHE_TTL_SECONDS).toBe(24 * 60 * 60)` AND `.toBe(86400)` (both forms — the named arithmetic catches `12 * 60 * 60`, the literal catches `24 * 60` ("24m"); either drift silently doubles or 60xs SF API spend until the next deploy notices). Methodology §3 documents 24h — pin what the docs name. **Second: `KvRestCache.get` error-class fan-in + `set` best-effort swallow.** The implementation explicitly catches four error-classes inside `get` and fans them all into `null`; the existing suite covered only two (`result: null` and a synchronous fetch throw). The other three — non-2xx response, malformed-JSON body (200 + bad body via `await res.json()` throw), unparseable `body.result` string (200 + envelope + bad `JSON.parse(body.result)`), and the `body.result === undefined` half of the loose-equality check (distinct from explicit `null`) — were all unguarded. A refactor that narrowed any catch to a specific error type (or dropped one entirely) would let a transient KV outage crash `/api/reviews` after the user already paid for the SF API call. Plus the symmetric `set` best-effort swallow that had **no** test today — `route.ts` does `await cache.set(...)` after the SF walk; an unhandled rejection would 500 the response. Pinned via `await expect(cache.set(...)).resolves.toBeUndefined()` against a synchronously-throwing `fetchImpl`. **Third: `get` REST-pipeline header symmetry with `set`.** The existing `set` test pins URL trailing-slash strip, Bearer auth, and Content-Type; the `get` test was only pinned on body shape. A refactor that broke any of these on `get` (e.g. moved `headers()` out of one path) would still pass both existing suites and stay silent until L4.1 hit a live KV. Pin on `.get()` the same three properties symmetrically: `"https://kv.example/"` → `"https://kv.example"`, `Authorization: Bearer tok`, `Content-Type: application/json`. Suite grows from 9 it-blocks/3 describes to 17 it-blocks/5 describes (boundary tests got their own `describe` for `CACHE_TTL_SECONDS` plus a new `describe` for the error-class fan-in); suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 11 deepens further still still still still** (D-064): L11.6 lands a deepening of `tests/export-csv.test.ts` — the CSV writer that ships the user's actual data deliverable (the `feedback_csv_ascii_for_excel` Excel-on-Windows contract: UTF-8 BOM, CRLF, QUOTE_ALL). The existing 7 it-blocks across 3 describes cover the BOM/CRLF terminators, header presence + row count, QUOTE_ALL with embedded `"` + `\n`, unicode survival, the photo-URL multi-join via `__testing.rowFor`, and `csvFilename`'s data-vintage date. Three cross-cutting load-bearing concerns were unguarded — the three silent-regression surfaces of a positional CSV: column order, embedded-delimiter survival, and edge-case shape. **First: `CSV_COLUMNS` exact-order freeze + per-row arity.** Downstream pipelines read the CSV by column position; a refactor that silently swapped `rating` ↔ `language` (or added a column in the middle) would shift every following cell across every existing consumer with **no runtime signal** — the file still opens in Excel, all cells still appear, but every "fifth-column lookup" downstream gets wrong data. Pin the exact 14-element ordered tuple (so any schema change is a reviewed change), pin the header row parses to that same tuple in surfaced order (the writer's header-emission path), and pin **every** emitted row carries exactly `CSV_COLUMNS.length` cells via parser round-trip (so a refactor that added/dropped a cell on one row misaligns loudly, not silently). **Second: embedded CSV-danger characters survive QUOTE_ALL escaping — all four, not just two.** The existing suite proves embedded `\n` + embedded `"` survive in `text`; it does **not** prove embedded **comma** (the field delimiter — under QUOTE_MINIMAL the cell would still be quoted, but under a refactor that dropped quoting entirely a single comma in a user's review would split the row) or embedded **CR** (half of the row delimiter — a naive splitter would mid-row terminate). Pin one review whose `text` carries all four characters and assert (a) parser round-trip yields the original string intact, (b) the resulting row count is exactly header + 1 (embedded CR/LF didn't split into more rows), and (c) the cell count is exactly `CSV_COLUMNS.length` (embedded comma didn't shift columns). Plus a comma-only follow-up that pins downstream column values (rating, published_at) by value so a column shift fails loudly at a known cell, not vacuously at a substring match. **Third: empty reviews shape + single-photo `photo_count` boundary + filename slice ceiling.** (i) An empty `payload.reviews` (brand-new place, no reviews yet) must still emit a valid Excel file: BOM + header + trailing CRLF — a regression that emitted just BOM or skipped the header on empty would break import with no error signal until a user opened the file. (ii) `photo_count` arity: the existing 2-photo case can't distinguish a 1-photo regression that emitted `url1 | ` (trailing PHOTO_URL_JOIN) from the correct `url1` (bare URL, no separator) — pin the 1-photo boundary so the join's "N>1 only" property holds. (iii) `csvFilename` uses `dateIso.slice(0, 10)`; a refactor to `slice(0, 8)` (the YMD digit count) or `slice(0, 11)` would misname every download. Pin the slice ceiling at the year/month/day boundary so either drift fails loudly. Suite grows from 7 it-blocks/3 describes to 15 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Suite-local minimal RFC-4180 parser handles the QUOTE_ALL output; not shipped. Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 11 deepens further still still still** (D-063): L11.5 lands a deepening of `tests/seo-variants.test.ts` — the L3.1a registry suite that already proves shape (10 candidates, unique ids+slugs, URL-safe slug regex, every variant carries the route-rendered `h1`/`intro`/`metaTitle`/`metaDescription`) and the L3.1b publish gate (`publishedVariants()` empty pre-L3.1b, `findPublishedVariant` never surfaces an unpublished slug). Three cross-cutting load-bearing concerns were unguarded — the three silent-regression surfaces of a long-tail SEO registry: the URL, the SERP display copy, and the target keyword. **First: slug-id pair freeze (URL contract).** The `{id, slug}` mapping is both the `docs/seo-variants.md` cross-reference and the canonical URL of each variant once L3.1b publishes it; a regression that silently renamed a slug (e.g. `export-google-reviews-to-csv` → `export-google-reviews-csv`) would change the page's permanent URL, 404 every inbound link, and invalidate the L1.6b/corgi keyword score that picked it. The existing suite proves uniqueness and URL-safety in aggregate but nothing about *which* slugs ship; pin the exact 10 `[id, slug]` pairs in registry order so any change is a reviewed change. Paired with the group balance count (4 A-prefix + 3 B-prefix + 3 C-prefix ids — the `docs/seo-variants.md` grouping that "10 with unique ids" alone wouldn't catch if a Group A entry were silently swapped for a Group B). **Second: SERP-length ceiling on metaTitle/metaDescription.** Google truncates `<title>` at ~60 chars and `<meta description>` at ~160 chars in the SERP display (the Moz/Ahrefs published conservative thresholds); a refactor that bloated a title past 60 silently lops off the tail — the page still ships, the keyword still ranks, but CTR drops because the headline trails into an ellipsis with no runtime signal. Pin the ceiling at 60/160 with non-empty floors, plus a guard-the-guard "longest current title fits" assertion so the ceiling can't pass vacuously on an empty registry or a tabulation that dropped string content (the L11.3-style positive-round-trip pattern). **Third: topical-keyword presence on the three SERP-weighted surfaces.** The whole reason these variants exist is the "google reviews" long-tail keyword cluster (L1.6a); a refactor that templated the strings (e.g. `s/Google Reviews/customer feedback/g`) would silently turn every page into a topical mismatch for its target query, and the registry-shape block would still pass — strings are still non-empty and URL-safe. Pin "google" AND "review" presence (case-insensitive) on `metaTitle` AND `h1` AND `metaDescription` — the three surfaces a search engine actually weights, asserted independently so a partial regression on any one fails loudly. (The `intro` paragraphs are deliberately *not* asserted: B3's `intro[0]` legitimately omits "google" — the body copy is allowed to vary as long as the SERP-visible surfaces lock the keyword.) Suite grows from 5 it-blocks/2 describes to 13 it-blocks/5 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 11 deepens further still still** (D-062): L11.4 lands a deepening of `tests/sf-client.test.ts` — the SemanticForce client suite that already proves fixture fallback, page-walking the 500-review LARGE fixture, HttpClient URL+Bearer wiring, and the helpers (`clampLimit`/`pickFixture`/`encodeCursor`/`decodeCursor`/`mapStatusToCode`). Three cross-cutting load-bearing concerns were unguarded. **First: FixtureClient page-boundary slicing.** `hasMore = nextOffset < fixture.reviews.length` is the off-by-one switch that decides whether `next_cursor` ships; the existing 500-review walk satisfies "every id seen exactly once" via set-size, but **500 is exactly divisible by 100**, so a regression that emitted a spurious tail cursor (`<=` instead of `<`) or swallowed a partial last page (`>` instead of `<`) would still satisfy the LARGE-fixture walk and ship silently. Pinned on the SMALL fixture in two passes: limit=5 over 12 reviews returns 5+cursor, 5+cursor (cursor must advance — same cursor twice would loop a caller infinitely), 2+**no cursor** (the partial-tail "we're done" signal); limit=12 exactly over 12 reviews returns one page + **no cursor** (the exact-fit "no spurious extra RTT" signal). **Second: HttpClient URL hygiene — what the wire must NOT contain.** Two silent-SF-rejection regressions with no test signal otherwise (we'd see an opaque 4xx from SF in L4.1): (a) a refactor that forwarded `args.limit` directly to the URL (skipping `clampLimit`) would emit `limit=9999` and SF would reject — pinned with `getReviews({limit: 9999})` → URL `limit=100`, the clamped value; (b) a refactor that set `after=undefined` always (no `if (args.after)` guard) would serialise the literal string "undefined" as the cursor — pinned belt-and-braces with both `seenUrl.searchParams.has("after") === false` and `seenUrl.toString().not.toContain("after=")`. **Third: `decodeCursor` offset-field guards.** The existing helper test pins the outer try/catch (garbage base64 → 0, missing → 0); the inner `typeof parsed.offset === "number" && parsed.offset >= 0` guard was unpinned, so a refactor that loosened it to `parsed.offset ?? 0` would let `-5` through and `Array.slice(-5, …)` silently slices "from the end" — a fixture-page corruption with no error signal. Pinned with `{offset: -5}`, `{offset: "12"}`, `{offset: null}`, `{}` (missing field) all → 0, plus a positive `{offset: 7}` → 7 round-trip so the negatives can't pass vacuously on a broken decoder. Suite grows from 13 it-blocks/3 describes to 18 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 11 deepens further still** (D-061): L11.3 lands a deepening of `tests/place-id.test.ts` — the canonicalisation engine that decides cache keys (D-020 `gr:reviews:v1:<slug>`), share-URL segments, and download filenames (D-033). The single-rule suite already proves each canonicalisation arrow in isolation (ChIJ recased, 0x lowercased, MOCK_ uppercased, URL extraction, whitespace trim, short-link rejection); three cross-cutting load-bearing concerns were unguarded. **First: surface-form aliasing → identical `{raw, slug}`.** This is the actual cache-key integrity contract — different surface forms of the same Place ID *must* collapse to one cache key, otherwise the KV entry fragments into one record per surface form (one for the lower-prefix paste, one for the whitespaced paste, one for the URL-embedded paste…) and the download filename changes between visits, breaking any automation that ingests by name. Pinned by asserting `.toEqual()` between the canonical `REAL_ID` and four equivalent forms (lower-prefix `chij…`, whitespaced `\t  …\n`, embedded in a long Google Maps URL, plus separate aliasing across the MOCK_ family (upper/lower/mixed) and the 0x family (upper-hex vs lower-hex). **Second: canonical-form idempotency.** `normalisePlaceId(normalisePlaceId(x).raw)` must equal `normalisePlaceId(x)` — the cache writes a key once and reads it many times; if the canonical `raw` weren't itself a fixed point, a normalise → store → re-normalise → fetch loop would silently miss its own writes. Started from a non-canonical input (lower-prefix `chij…`) so the second pass actually exercises the canonicalisation arrow rather than passing vacuously, then extended over MOCK_ and 0x families. **Third: PLACE_ID_PATTERN minimum-length floor.** `ChIJ[\w-]{20,}` and `0x[0-9a-fA-F]+:0x[0-9a-fA-F]+` and `MOCK_[A-Z0-9_]+` carry per-family length floors; a refactor that loosened `{20,}` to `{1,}` or replaced `+` with `*` would let almost-real stubs through and the live SF call would 404 with no validation signal at our edge — pin the floor at the boundary so the regression reads as a red test, not a confusing upstream 404. Pinned with ChIJ + 19 tail chars rejected, ChIJ + 20 tail chars accepted (the smallest match — both ends of the boundary asserted so a future `{19,}`/`{21,}` flip fails loudly), `0x:0xABCD` and `0xABCD:0x` both rejected, bare `MOCK_` (no body) rejected. Suite count still 19 files (deepening one, no new file); place-id suite grows from 13 it-blocks across 3 describes to 22 it-blocks across 6 describes. Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 11 deepens further** (D-060): L11.2 lands a deepening of `tests/api-reviews.test.ts` — the route's HTTP-contract suite already covered the 400 envelope and the 200 MISS Content-Type, but two load-bearing concerns were unguarded. **First: the `userLimit` slicing across all three formats.** `app/api/reviews/route.ts` slices `payload.reviews.slice(0, userLimit)` in `respondSuccess` for every format; a regression that skipped the slice (or flipped the loop guard) would silently return all 12 fixture reviews when the user asked for 3 — a download larger than the user authorised, with no error to surface it. L11.2 asserts from the *response* (not the route helper) so a refactor that moved the slice elsewhere still has to satisfy it: `?limit=3` → JSON `reviews.length === 3`, CSV body has exactly 4 non-empty CRLF-separated lines after BOM strip (header + 3 data), XLSX round-trips through `XLSX.read` → `sheet_to_json({ header: 1 })` to exactly 4 cell rows; plus the three boundary cases (`?limit=99999` past the 12-review fixture returns all 12 not garbage — pinning that `Array.slice` safety carries through; `?limit=3.7` floors to 3, the documented `Math.floor` intent). **Second: the slug-in-filename contract.** `csvFilename`/`xlsxFilename` produce `google-reviews-<slug>-<YYYYMMDD>.<ext>` (ADR-003), and the route passes `normalised.slug` — *not* `normalised.raw` or the raw `placeIdInput` — so a refactor that swapped the argument would silently rename every user's download and break any automation that ingests files by name. Pinned with a mixed-case underscore input (`mock_SMALL_001`) so the slug transform (lowercase + underscores→dashes) is what surfaces: filename must match `google-reviews-mock-small-001-\d{8}\.(csv|xlsx)` *and* must **not** contain the underscore form (substring matches on "mock" alone would pass on the raw form too, so the dashed/lowercased form is the load-bearing assertion). The X-Cache: HIT path was considered and deliberately deferred: each `createReviewsCache()` call returns a new `MemoryCache` in fixture mode (no module-level singleton), so HIT is unreachable through the public route surface without either a singleton (production-behaviour change in memory mode) or a route-level injection seam (the L8.5 precedent allowed, but heavier than this leaf needs — leave it for a dedicated future leaf). Suite count now 19 files, api-reviews suite grows from 7 it-blocks across 3 describes to 14 it-blocks across 5 describes. Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 11 — Suite-deepening tranche opened** (D-059): with L10.1 merged (#55) every standard Phase 0–10 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and per D-058 **every runtime module *and* the underlying fixture data** has a dedicated suite — so per D-046/D-051 the routine opened a roadmap-adjustment rather than idle. The candidate D-057/D-058 explicitly named for deepening was `tests/faq-jsonld.test.ts`, which exercises `FAQ_ITEMS` + `faqJsonLd()` but never invokes the visible `<FaqSection />` renderer; that left two load-bearing render-side contracts unguarded — the `aria-labelledby="faq-heading"` ↔ `<h2 id="faq-heading">` pair (silent screen-reader break if either side drifts, the visible heading dissociates from the region), and the rich-vs-plain divergence-by-design (on-page renders `item.a` rich JSX with `<code>429</code>` etc.; JSON-LD ships `item.text` plain — a refactor that swapped FaqSection to render `item.text` would silently collapse the two paths and the parity suite would not catch it, both copies still contain "429" as a substring). L11.1 lands `tests/faq-section.test.ts` — sibling file to `faq-jsonld.test.ts` so file names stay one-concern-per-file: exactly one `<section aria-labelledby="faq-heading">` paired with exactly one `<h2 id="faq-heading">FAQ</h2>` (both ends pinned); exactly `FAQ_ITEMS.length` `<details>` entries in surfaced order (nth `<summary>` contains nth `item.q`, the L6.5/D-046 check-order spirit so visible accordion order can't desync from the JSON-LD `mainEntity` order); all default-closed (no stray `open` — a layout regression the design rejects); and the rich-vs-plain proof — a `<code>` element exists inside the rate-limits `<details>` (only `item.a` carries `<code>` markup, never `item.text` — substring matches on "429" would still pass after the swap, structural existence fails loudly), with guard-the-guard that the visible flatten reads as real glyphs (no surviving `&apos;`/`&amp;`/`&quot;`, the JSX runtime converts them before they leave the element). Same D-050 no-react-dom structural walk as review-tool-form / variant-page / root-layout. Suite count now 19. Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 10 — Fixture & data-contract hardening opened** (D-058): with L9.1 merged (#54) every standard Phase 0–9 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2) and **every runtime module now has a dedicated suite**, so per D-046/D-051 the routine opened a roadmap-adjustment rather than idle. The one cross-cutting surface still unguarded is the committed `mocks/semanticforce/` fixtures themselves — `client.ts` imports each JSON `as unknown as Fixture`, so TypeScript checks nothing about the data every other suite and the whole mock-first contract depend on. L10.1 lands `tests/fixtures-contract.test.ts` (validated through `client.__testing.FIXTURES`, the runtime-served objects): registry shape (exactly the three `MOCK_*_001` keys; `place.place_id` == registry key), committed sizes 12/80/500 (the **committed reality** — the L1.3 prose's "small (10)" is stale; the file is 12, the number the preview suite hard-codes per D-041), `rating_count >= reviews.length` for every fixture (data-source side of the D-041 total-not-walk-count invariant), the full `Review`/`PlaceMeta` per-record shape (unique non-empty `review_id`, integer `rating` 1..5, non-empty `author_name`/`text`, ISO-8601-UTC `published_at`, well-formed optional `photos`/`owner_response`/`language`), and the L1.3-promised coverage surviving (full 1..5 spread, ≥4 languages, ≥1 photo + owner response, non-ASCII text — the `feedback_csv_ascii_for_excel` memory). Suite count now 18 files (the L9.1 prose's "18" ran one ahead — root-layout was the 17th file; this is the 18th). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — **Phase 9 — Layout & shell hardening opened** (D-057): with L8.5 merged (#53) every standard Phase 0–8 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), so per D-046/D-051 the routine opened a roadmap-adjustment rather than idle. The last runtime module without a dedicated suite was `app/layout.tsx`. L9.1 lands `tests/root-layout.test.ts` — pins the env-gated Plausible analytics snippet (L2.10) from **both** sides: `RootLayout` renders the `next/script` `<Script>` only when `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set (canonical plausible.io `src`, `data-domain` == the env value asserted with two distinct values so it can't pass on a constant, `defer`, `strategy: afterInteractive`) and renders **no** analytics script when it is unset — the privacy/opt-in regression caught both ways (an unconditional snippet tracks every build before the human opts in; a never-rendered one collects nothing despite the var). The `<Script>` is matched by reference identity against the same `next/script` import the layout uses (module identity holds), not by a fragile component `.name` (next/script's default is a forwardRef object). Also pins the document shell: a single `<html lang="en">`, a single `<body>` carrying the `bg-background`/`text-foreground`/`min-h-screen` design tokens, and `children` threaded through into the body via a sentinel child (not dropped, not double-wrapped). Env captured/restored per-test (D-044 pattern, as seo-routes.test); no-react-dom structural walk (D-050). With L9.1 **every** runtime module now has a dedicated suite. Same committed-not-run posture (D-039/D-040/D-042). Suite count now 18. Earlier history follows. — L8.4 lands `tests/cn-util.test.ts` (D-055) — pins both halves of the `cn()` helper in `lib/utils.ts`: the clsx layer (string/array/object/conditional inputs joined; every falsy entry — `false`/`null`/`undefined`/`0`/`""` — dropped; empty result is `""`) and the tailwind-merge layer (later conflicting Tailwind utility wins, `cn("p-2","p-4")==="p-4"`, including a conflict introduced via a conditional; non-conflicting utilities all survive in order; an exact repeat dedupes). 11 cases over a pure no-I/O function depended on by every component, locking class-merge behaviour against a clsx ^2.1.1 / tailwind-merge ^2.5.5 bump. Suite count now 16. Same committed-not-run posture (D-039/D-040/D-042). Next agent-doable leaf is L8.5 (the D-048/D-051 healthcheck client-injection seam — the one Phase 8 leaf that touches production code). Earlier history follows. — L8.3 lands `tests/home-page.test.ts` — pins the `app/page.tsx` composition contract (D-054): HomePage renders exactly one shared `<ReviewToolForm/>` and exactly one shared `<FaqSection/>` (counted by component-function name via the D-050/D-053 structural walk, so an inline home-only form/FAQ that would drift from the variant pages' shared copy and break the D-040 visible/structured-data parity reads as 0 and fails), inside a single `<main>` landmark. Metadata is pinned as *inherited, not page-local*: `app/page.tsx` exports no `metadata` of its own (a stray one would silently shadow the root layout — the absence is asserted via `"metadata" in (import * as page)` being false), and the effective metadata read off `app/layout.tsx` still carries the expected non-empty title and a description that names CSV/JSON/XLSX. The D-041 "ships in L2.5" placeholder is pinned gone via a deep flatten that *invokes* the pure hookless sub-components (ReviewToolForm, FaqSection, the FAQ items' rich-JSX `a`) so reintroduced placeholder text anywhere in the composed page — not just the page file — fails, paired with a positive `/download every\s+review/i` assertion so the negatives can't pass vacuously on an empty flatten (guard-the-guard). Suite count now 15. Same committed-not-run posture (D-039/D-040/D-042; D-050). Earlier history follows. — Phase 8 — Render-contract hardening **opened** (D-052): every standard Phase 0–7 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), so per D-046/D-051 the routine opened a Phase 8 tranche over the still-untested component/page modules (`app/(seo)/[variant]/page.tsx`, `app/_components/review-tool-form.tsx`, `app/page.tsx`, `lib/utils.ts`) plus the D-048/D-051-named healthcheck client-injection seam, and is landing one leaf per module. L8.2 lands `tests/review-tool-form.test.ts` — pins the shared tool form's post-D-041 shape: it is a single `<form action="/preview" method="GET">` (with redundant negatives — `action` not-contains `/api/reviews`, `target` undefined — that read as the silent-revert threat they guard, D-051 precedent), carries exactly one `required` text input `name="placeId"`, and renders exactly three `name="format"` radios whose values equal `["json","csv","xlsx"]` in surfaced order with exactly one `defaultChecked` and it is `json` (the documented preferred-download default that rides to the preview CTA, D-041; ordered-array assertion is the L6.5/D-046 check-order spirit). It is the simplest D-050-family suite: `ReviewToolForm` is pure/synchronous/hookless with only intrinsic JSX elements, so it is invoked once and the returned tree walked structurally by `el.type` strings — nothing to flatten, no esbuild-name fragility (D-053). Suite count now 14. Same committed-not-run posture (D-039/D-040/D-042; D-050). Earlier history follows. — L8.1 lands `tests/variant-page.test.ts` — pins the L3.1a inert-until-L3.1b contract for `app/(seo)/[variant]/page.tsx`: `dynamicParams === false`, `generateStaticParams()` equals `publishedVariants()` mapped to `{variant:slug}` (empty pre-L3.1b — the assertion that flips when L3.1b publishes the top-5, mirroring the seo-variants/seo-routes pre-L3.1b note, D-044), `generateMetadata` returns `{}` for both a real-but-unpublished registry slug and a garbage slug, and `VariantPage` calls `notFound()` for **every** registry slug (so a refactor that widened the lookup past the `published` gate ships ten live pages and fails loudly). `notFound()` is asserted via the thrown Next sentinel, tolerant on the exact `digest` string (`/NOT_FOUND|404/`, D-043 freeze-guard precedent) so a Next bump can't false-fail the real "this slug does not render" contract; the published-render path is genuinely unreachable pre-L3.1b without mutating the frozen registry or mocking, so it is documented (D-048 posture) as an L3.1b-era follow-up where the D-050 structural-walk/flatten technique applies. Suite count now 13. Same committed-not-run posture (D-039/D-040/D-042; D-050). Earlier history follows. — Phase 7 — Coverage hardening **complete**. L7.5 lands `tests/seo-routes.test.ts` — the `app/robots.ts` + `app/sitemap.ts` contract: robots is exactly one wildcard rule with `allow: "/"` and `disallow` containing **only** `/api/` (and never `/`, the whole-site-deindex footgun); both routes resolve the origin from `NEXT_PUBLIC_SITE_URL` with the documented `https://googlereviewsdownload.com` fallback (D-035) — override honoured with a single trailing slash stripped and surrounding whitespace trimmed, blank/whitespace-only → fallback; `robots().sitemap`/`host` and the sitemap's absolute URLs all carry that base; and `sitemap()` enumerates **exactly** root + `publishedVariants()` (root `weekly`/priority-1, variants `monthly`/priority-0.8), pinned root-only pre-L3.1b so the sitemap is byte-stable until the reviewed L3.1b publish flip (mirrors the seo-variants suite's pre-L3.1b note — when L3.1b lands, the length assertions are the intended change, D-044). `NEXT_PUBLIC_SITE_URL` captured+restored per-test (D-044 env pattern) so a host that exports it can't silently route the fallback assertions down the override path. With L7.5 done **every standard Phase 0–7 leaf is `[x]`** except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2) — the next routine run has no agent-doable standard leaf and must open a roadmap-adjustment (a further hardening tranche or an explicit idle-justification note) rather than idle (D-046/D-051). Same committed-not-run posture (D-039/D-040/D-042; D-050). Earlier history follows. — L7.4 lands `tests/preview-route.test.ts` — the result-preview (`app/preview/page.tsx`) contract: `metadata.robots` is `noindex` (a per-query throwaway URL must never be indexed); the page renders the **first ≤5** reviews regardless of fixture size; the displayed total is `place.rating_count` and **not** a walk/fetch count (D-041/D-031) — pinned from both sides with the small fixture (rating_count 12 ≠ 5 rendered rows) and the large fixture (rating_count 609 ≠ 5 rows ≠ 500 full-fixture length, so a "count what we fetched" regression can't hide behind a coincidentally-equal number); and bad input (missing/blank/goo.gl-short-link/garbage `placeId`) renders an error *card* with zero review rows, never a thrown crash. Driven with **no react-dom**: `PreviewPage` is awaited (Next 15 async `Promise<searchParams>`), then the returned React element tree is traversed two ways — a *structural* walk (descend `props.children` without invoking function components) counts `ReviewRow` elements by `type.name` since they sit directly in the returned tree, and a *deep* flatten invokes the pure, hookless sub-components (Shell/PlaceHeader/DownloadCta/ErrorCard/ReviewRow) to collect visible text, with `next/link`'s forwardRef Link descended structurally so the next runtime is never needed; a whitespace-collapsing `norm()` guards substring assertions against JSX-expression-boundary spacing in the flatten. `SF_API_KEY`/`SF_API_BASE` neutralised per-test (D-044 pattern) so `createSemanticForceClient()` always uses the committed FixtureClient and the fixture is selected purely by the passed placeId. Same committed-not-run posture (D-039/D-040/D-042; D-050). Earlier history follows. — L7.3 lands `tests/faq-jsonld.test.ts` — the first suite to import a `.tsx` module: it pins the FAQ visible/structured-data parity that L3.2b/L3.3 built into `app/_components/faq.tsx`. `faqJsonLd()` must be a valid schema.org `FAQPage` (`@context`/`@type`/`mainEntity`), carry exactly one `Question` per `FAQ_ITEMS` **in order** with the matching `name`, and — the core invariant — `acceptedAnswer.text` must equal each item's plain-text `text` *exactly*, with that `text` proven to be genuine prose (no surviving `<…>` tags, no unescaped JSX entities `&apos;`/`&amp;`/`&quot;` — guarding the entity-vs-glyph drift between the rich-JSX `a` and the JSON-LD mirror). Also: JSON round-trip stability (it ships via `JSON.stringify` into a `<script type="application/ld+json">`) and a fresh-object-per-call check. Importing `faq.tsx` forced a `vitest.config.ts` change: tsconfig is `jsx: "preserve"` (Next compiles JSX), but Vitest has no Next pipeline, and `FAQ_ITEMS`' module-level JSX in each `a` field evaluates at import time — so `esbuild.jsx: "automatic"` + `jsxImportSource: "react"` was added, auto-importing `react/jsx-runtime` (react is a dep) with no React global and no source change; the suite never renders the elements (D-048). Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — L7.2 lands `tests/healthcheck-route.test.ts` — the SF liveness-probe contract for `app/api/healthcheck/route.ts`: the ok path (SF_API_KEY unset → FixtureClient → `MOCK_SMALL_001` has place metadata → status `ok` → **200**, `mode: "fixture"`, no `error`), the always-`Cache-Control: no-store` invariant (a cached health probe is a lie), `latency_ms` numeric+non-negative, `place_id` == the probe id, and `checked_at` as a true round-tripping ISO-8601 string. The only env-reachable failure branch is exercised as the `down` case: SF_API_KEY set **without** SF_API_BASE makes `createSemanticForceClient()` throw a `SemanticForceError("bad_request")` the route catches at init → **503**, `status: "down"`, `error.code: "bad_request"`, and crucially `mode: "live"` (proving the inference is pure SF_API_KEY presence, even on the failure path). The `degraded` branch and the `getReviews()`-throws → `down` branch are **not reachable through env** — the route calls `createSemanticForceClient()` with no options so there is no client-injection seam, and the suite stays no-mock (D-044) rather than introducing `vi.mock` to force them; the limitation is documented in the suite header, and closing it needs a route-level injection seam (a future leaf), not a test hack. `__testing.PROBE_PLACE_ID` pinned so an L4.1 real-place swap is a reviewed change. Same committed-not-run posture (D-039/D-040/D-042). Earlier history follows. — Phase 7 — Coverage hardening — opened per D-046/D-047: with every standard Phase 0–6 leaf `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), a routine run had no agent-doable standard leaf, so rather than idle it added a Phase 7 tranche of regression suites for the five still-untested runtime modules (`middleware.ts`, `app/api/healthcheck`, `app/_components/faq.tsx`, `app/preview`, `app/robots.ts`+`app/sitemap.ts`) and landed its first leaf. L7.1 lands `tests/middleware-ratelimit.test.ts` — the edge rate-limit (L2.8) abuse surface: `identify` IP resolution (XFF-leftmost › x-real-ip › "unknown", route-scoped key), token-bucket arithmetic (exactly `BUCKET_CAPACITY` in a burst, the +1th denied, one token back after exactly `RETRY_AFTER_SECONDS` and not a tick before, refill clamped at capacity, per-key isolation) and opportunistic eviction proven via a recording store that counts `delete` calls (eviction vs long-gap refill are observationally identical through the return value alone — D-047). Driven by an injected store + explicit `now` (no real timers, no shared module Map) and a real `NextRequest` for `identify`; committed-not-run (D-039/D-040/D-042 posture). Earlier history follows. — L6.5 closes Phase 6's last agent-doable leaf: `tests/api-reviews.test.ts` guards the `/api/reviews` HTTP contract — the 400 `bad_request` envelope for missing-placeId/bad-format/bad-limit/unparseable-placeId, the check-**order** invariant (placeId-before-format, the order the form surfaces), and the success-path Content-Type/Content-Disposition for json/csv/xlsx + case-insensitive format. Handler is driven by a real `NextRequest` (route reads `req.nextUrl.searchParams`); SF_API_KEY + KV_* neutralised per-test so fixture/memory fallbacks are deterministic. Committed-not-run (D-039/D-040 posture). With L6.5 done, every standard Phase 0–6 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2) — the next routine run has no agent-doable standard leaf and should open a roadmap-adjustment (a Phase 7 of further hardening, or a no-op note) rather than idle — D-046. Earlier history follows. — L6.4 adds `.github/workflows/ci.yml` — a non-blocking informational quality gate on PRs into `main`: checkout → setup-node@20 (npm cache) → install → `npm run typecheck` → `npm run lint` → `npm test`. It runs in parallel with `auto-merge-claude.yml` (which squash-merges `claude/*` PRs on open without waiting for checks, so CI never blocks the burn pipeline) and records a real pass/fail status a human can later promote to a required check via branch protection — no `continue-on-error` by design. Install step prefers `npm ci` but falls back to `npm install --no-audit --no-fund` because the repo is intentionally manifest-only with no committed `package-lock.json` (D-039/D-040 posture); a literal `npm ci` would be permanently red — D-045. Earlier history follows. — L6.3 lands `tests/sf-client.test.ts` + `tests/seo-variants.test.ts`: the SF client suite neutralises `SF_API_KEY`/`SF_API_BASE` per-test so the no-key→FixtureClient fallback is deterministic on any host, page-walks the 500-review large fixture (5×100, 500 unique `review_id`s, zero overlap) to lock the `decodeCursor`/slice cursor contract, and exercises HttpClient via an injected `fetchImpl` stub (URL shape, `Bearer` header, trailing-slash strip, status→error-code mapping, network-throw→`upstream_error`) plus `__testing` helper coverage (clampLimit/pickFixture/encode-decodeCursor/mapStatusToCode); the variants suite locks registry invariants (10 candidates, unique ids+slugs, URL-safe slug regex, every variant carries the route-rendered `h1`/`intro`/`metaTitle`/`metaDescription`) and the publish gate (`publishedVariants()` ⊆ registry, currently empty pre-L3.1b with an inline note that flipping it is the reviewed L3.1b change, `findPublishedVariant` never surfaces an unpublished slug) — D-044. Same committed-not-run posture as L6.1/L6.2. Earlier history follows. — L6.2 lands the next two regression suites — `tests/export-xlsx.test.ts` (round-trips `formatReviewsAsXlsx` output through `XLSX.read`: one "Reviews" sheet, one row/review, 14-col `!ref`, `!cols` widths == `COLUMN_WIDTHS`, frozen-header guard tolerant of `!freeze` vs `!views` on read — D-043) and `tests/reviews-cache.test.ts` (versioned `cacheKey`, `MemoryCache` TTL expiry + eviction via an injected `now`, `KvRestCache` SET/GET pipeline body + bearer headers + trailing-slash strip via a recording `fetchImpl` stub, miss/throw paths). Same committed-not-run posture as L6.1. Earlier history follows. — Phase 6 — Hardening & quality — added (D-042): every standard Phase 0–5 leaf is now done except corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), so this run opened an agent-doable hardening phase rather than idling. L6.1 lands the first automated tests: a `vitest` harness (`vitest.config.ts` with the tsconfig `@/*` alias mirrored, `node` env, `tests/**/*.test.ts`), `test`/`test:watch` scripts + `vitest` devDep in `package.json`, and regression suites `tests/place-id.test.ts` + `tests/export-csv.test.ts` exercising the existing `__testing` hooks on the two purest, highest-stakes modules — Place-ID canonicalisation (cache-key/filename integrity) and the CSV Excel contract (BOM/CRLF/QUOTE_ALL, the `feedback_csv_ascii_for_excel` memory). Tests are committed but not executed in-routine: repo is manifest-only/no `node_modules` and `npm install` is a human step (same posture as D-039/D-040); they run on `npm install && npm test`. Earlier history follows. — L2.5 result preview rebuilt cleanly off `main` (old PR #20 was CONFLICTING/closed): new server route `app/preview/page.tsx` fetches the first 5 reviews via the SF client server-side, shows place meta + `rating_count` total + per-format download CTA, and `robots: noindex`; shared `app/_components/review-tool-form.tsx` rewired from `target=_blank` raw `/api/reviews` to a same-tab `/preview` GET (`format` carried as preferred-download); stale "ships in L2.5" placeholder removed from `app/page.tsx` and the variant route; preview skips KV walk + L2.8 rate-limit by design (D-041). L3.2b lands JSON-LD `FAQPage` on variant pages via shared `app/_components/faq.tsx` — paired rich-JSX/plain-text FAQ used by both home and variant pages so visible copy and structured data can't drift; home page refactored onto it with no content change. L3.1 split into L3.1a — agent-half variant-page infrastructure: `lib/seo/variants.ts` 10-candidate registry with `published` flag, shared `app/_components/review-tool-form.tsx` extracted from the home page, dynamic `app/(seo)/[variant]/page.tsx` with `dynamicParams = false`, sitemap enumerates published variants; all `published: false` so everything is inert/404 until L3.1b — and L3.1b — corgi-gated, flips top-5 to published after the L1.6b volume pass; L2.8 edge rate-limit middleware landed via consolidated rebuild; L4.2 adds `app/api/healthcheck/route.ts` — edge route that pings SF via the shared client with `MOCK_SMALL_001`/`limit:1`, reports `status` (ok/degraded/down), `mode` (fixture/live, inferred from `SF_API_KEY`), and `latency_ms`; 200 when ok, 503 otherwise, `Cache-Control: no-store`; mock-safe so it runs before the human-gated L4.1; L5.4 lands `docs/outreach-plan.md` — per-channel community outreach plan for after launch: Sterling Sky Local Search Forum, r/bigseo, LocalU, r/SEO, r/smallbusiness, Traffic Think Tank, AgencyAnalytics Slack, Search Engine Roundtable, Twitter/X, LinkedIn groups; pre-flight gates on L5.2 deploy + L2.8 rate-limit + L2.10 analytics + L4.1 real creds; one channel per weekday cadence with UTM tagging for Plausible attribution; explicit skip list (BHW, Warrior Forum, FB groups); L3.3 FAQ on `app/page.tsx` — three `<details>` items — unblocks FAQ-content half of L3.2b; L5.3 launch posts in `docs/launch-posts.md`; L3.2 split into L3.2a — robots.ts + sitemap.ts for root — and L3.2b — JSON-LD `FAQPage` deferred until L3.1; L2.10 Plausible snippet env-gated; L2.9 launch-checklist landed; L2.5 has PR #20 with a merge conflict needing human rebase; L1.6b deferred for local corgi pass)

Leaf-task granularity. Each leaf should fit in **one scheduled run (≤10 commands)**. The routine picks the next unchecked leaf top-down. Mark `[x]` when merged, `[~]` when draft PR open awaiting review, `[!]` when blocked.

---

## Phase 0 — Planning bundle (Sprint 0)

- [x] L0.1 Write `PLAN.md`, `ROADMAP.md`, `ROUTINE.md`, `DECISIONS.md`, `LEDGER.md`
- [x] L0.2 Bootstrap commit on `main` (no PR — empty repo)
- [x] L0.3 Mirror to Obsidian `GOOGLE-REVIEWS-DOWNLOAD/`
- [x] L0.4 Write `docs/semanticforce-api.md` stub + `mocks/semanticforce/` fixture skeleton
- [x] L0.5 Schedule the 04:00 Madrid autonomous routine

## Phase 1 — Foundation (Sprint 1, ~5 daily runs)

- [x] L1.1 Add `package.json` with Next.js 15 + TS + Tailwind + shadcn/ui + `xlsx` (manifest only — no install)
- [x] L1.2 Add `.gitignore`, `.env.example` (lists `SF_API_KEY`, `SF_API_BASE`, `KV_*`), `tsconfig.json`, base `next.config.ts`
- [x] L1.3 Flesh out `mocks/semanticforce/`: small (10 reviews), mid (80), large (500). Include unicode, multi-language, varying star distributions, photo URLs.
- [x] L1.4 Implement `lib/semanticforce/client.ts` with the type interface and a fixture-fallback when `SF_API_KEY` is unset. Add `lib/semanticforce/types.ts` with `Review`, `PlaceMeta`, `GetReviewsResponse`.
- [x] L1.5 Write `docs/methodology.md` — how `place_id` is normalised (slug + raw), how pagination is handled, how cache keys are constructed, how rate-limiting is enforced
- [x] L1.6a Long-tail seed (agent half): write `docs/seo-variants.md` with ~10 candidate variants grouped by intent + per-variant rationale, **no volumes**. Volumes are L1.6b.
- [ ] L1.6b Long-tail seed (corgi half): run `corgi keywords ...` locally on the L1.6a variants and paste volume + competition columns back into `docs/seo-variants.md`. **(deferred: needs local corgi pass — human-gated, real-cash spend tracked in LEDGER.md.)**
- [x] L1.7 Write `DECISIONS.md` ADRs for stack, mock-first contract, export-format defaults

## Phase 2 — Core MVP (Sprint 2, ~6 daily runs)

- [x] L2.1 Scaffold Next.js app skeleton (commit code only; no install)
- [x] L2.2 Implement `app/api/reviews/route.ts` calling the SF client; query params: `placeId`, `format` (csv/json/xlsx), `limit?`
- [x] L2.3 Implement KV cache layer keyed by normalised `place_id`, 24h TTL
- [x] L2.4 Build the input form `app/page.tsx` — single field, format toggle, download button
- [x] L2.5 Build the result preview component (first 5 reviews + total count + download CTA) — rebuilt cleanly off `main` as server route `app/preview/page.tsx`; form rewired paste→preview→download; old conflicted PR #20 superseded
- [x] L2.6 Implement CSV writer in `lib/export/csv.ts` — UTF-8 BOM, CRLF, QUOTE_ALL (per `feedback_csv_ascii_for_excel`)
- [x] L2.7 Implement XLSX writer in `lib/export/xlsx.ts` using `xlsx`; one row per review, frozen header, sensible column widths
- [x] L2.8 Add edge rate-limit middleware (token-bucket per IP, e.g. 10 req/min)
- [x] L2.9 Write `docs/launch-checklist.md` for human sign-off before deploy
- [x] L2.10 Add Plausible analytics snippet (env-gated)

## Phase 3 — SEO surface (Sprint 3, ~3 daily runs)

- [x] L3.1a Build the SEO variant-page infrastructure (agent half, content-agnostic): `lib/seo/variants.ts` registry of all 10 L1.6a candidates with a `published` flag, shared `app/_components/review-tool-form.tsx` (extracted from `app/page.tsx` so home + variant pages share one tool), dynamic `app/(seo)/[variant]/page.tsx` (`dynamicParams = false`, custom intro above the fold, shared tool below), and `app/sitemap.ts` enumerating published variants. All variants `published: false` → routes 404 and sitemap unchanged until L3.1b.
- [ ] L3.1b (depends on L1.6b) Flip the corgi-picked top-5 variants to `published: true` in `lib/seo/variants.ts` (score `volume × (1 - competition)` from the L1.6b pass, manual override allowed for uniquely-weak SERPs). **(deferred: needs local corgi pass — gated behind L1.6b.)**
- [x] L3.2a Add `app/robots.ts` and `app/sitemap.ts` (root route only; variant routes get added in L3.1; `NEXT_PUBLIC_SITE_URL` env var with fallback)
- [x] L3.2b Add JSON-LD `FAQPage` schema on each variant page (shared `app/_components/faq.tsx`: `FAQ_ITEMS` with paired rich-JSX `a` + plain-text `text`, `FaqSection`, `faqJsonLd()`; home page refactored onto it with no content change; variant page emits `<script type="application/ld+json">` + visible `<FaqSection />` so markup matches on-page content).
- [x] L3.3 Add a "How it works" + "Is this allowed by Google?" FAQ to `app/page.tsx`

## Phase 4 — Real-creds integration (Sprint 4, ~2 daily runs — gated)

- [ ] L4.1 (**human-gated**, requires real SF creds) Update `client.ts` to call the live SF API; verify against fixtures; document any schema deltas
- [x] L4.2 Add a `/api/healthcheck` route that pings SF with a known place and reports latency

## Phase 5 — Launch prep (Sprint 5)

- [ ] L5.1 Naming + domain decision (target candidates: `googlereviewsdownload.com`, `.co`, `.app` — **human-gated** purchase)
- [ ] L5.2 Deploy to Vercel (**human-gated**, needs SF + KV creds)
- [x] L5.3 Draft launch posts in `docs/launch-posts.md` (ProductHunt, IndieHackers, LinkedIn)
- [x] L5.4 Outreach plan to local-SEO communities

## Phase 6 — Hardening & quality (agent-doable, no creds/deploys)

_Added because Phase 0–5 agent work is exhausted; only corgi/human-gated leaves remain. These need no SF creds, no deploy, no real-cash corgi — pure regression/quality work the routine can land cleanly._

- [x] L6.1 Add a `vitest` test harness (manifest + `vitest.config.ts` with the tsconfig `@/*` alias, `node` env) and the first regression suites for the two purest, highest-stakes modules: `tests/place-id.test.ts` (canonicalisation rules that decide cache keys/filenames, incl. D-018 short-link rejection + host-before-pattern precedence) and `tests/export-csv.test.ts` (the Excel BOM/CRLF/QUOTE_ALL contract + unicode survival + `csvFilename` data-vintage). Both drive the already-present `__testing` hooks. Committed, not run in-routine (no `node_modules`; `npm install` is a human step — D-039/D-040 posture).
- [x] L6.2 Add `tests/export-xlsx.test.ts` (worksheet shape, one row/review, frozen header `ySplit:1`, column widths) and `tests/reviews-cache.test.ts` (`cacheKey` prefix, `MemoryCache` TTL expiry via injected `now`, `KvRestCache` pipeline body shape with a stub `fetchImpl`). XLSX suite round-trips the produced bytes through `XLSX.read` and asserts sheet name/shape/`!ref`/`!cols` widths plus a freeze guard tolerant of whichever pane prop SheetJS 0.18.5 repopulates on read (D-043). Cache suite injects a `now` clock for deterministic TTL expiry and a recording `fetchImpl` stub for the KV REST pipeline body/headers — no real timers, no live KV. Committed, not run in-routine (D-039/D-040).
- [x] L6.3 Add `tests/sf-client.test.ts` (fixture fallback when `SF_API_KEY` unset; `limit`/`after` paging against the committed mocks) and `tests/seo-variants.test.ts` (registry invariants: unique slugs, `publishedVariants()` ⊆ registry, every published variant has the fields the route renders). Client suite pins/unsets `SF_API_KEY`/`SF_API_BASE` per-test for deterministic fixture fallback, walks the 500-review large fixture page-by-page (5×100, 500 unique ids, no overlap), and drives HttpClient through an injected `fetchImpl` stub (URL/Bearer/trailing-slash/status→code). Variants suite asserts `publishedVariants()` empty pre-L3.1b. Committed, not run in-routine (D-039/D-040/D-044 posture).
- [x] L6.4 Add a `.github/workflows/ci.yml` running `npm ci && npm run typecheck && npm run lint && npm test` on PRs to `main` (does not block the `claude/*` auto-merge workflow — informational status; human can later make it required). Shipped: `pull_request → branches: [main]`, `permissions: contents: read`, concurrency-cancel per ref, Node 20 + npm cache; `npm ci`-with-`npm install`-fallback because the repo is lockfile-free (D-045). Non-blocking by construction — auto-merge merges before CI finishes.
- [x] L6.5 Add `tests/api-reviews.test.ts` — param-validation + response-contract guard for `app/api/reviews/route.ts`: missing `placeId` → 400 `bad_request`, unsupported `format` → 400 listing supported formats, non-numeric/zero/negative `limit` → 400, unparseable placeId (goo.gl short link, D-018) → 400, and the **check-order** invariant (placeId presence before format validity, the order the form surfaces). Success path asserts the JSON envelope shape + `X-Cache: MISS` and the `Content-Type`/`Content-Disposition` headers for csv/xlsx (incl. case-insensitive `CSV`); plus `__testing.statusForCode`/`inferRetryAfter`. Exercised by importing `GET` and handing it a real `NextRequest` (the route reads `req.nextUrl.searchParams`, which a plain `Request` lacks); SF_API_KEY + KV_* neutralised per-test so the fixture/memory fallbacks are deterministic. Committed, not run in-routine (D-039/D-040 posture; D-046).

## Phase 7 — Coverage hardening (agent-doable, no creds/deploys)

_Opened per D-046/D-047: every standard Phase 0–6 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), so the routine added agent-doable regression work rather than idling. Phase 6 covered the pure library modules; Phase 7 closes the gap on the five still-untested runtime modules. Same committed-not-run posture as Phase 6 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L7.1 Add `tests/middleware-ratelimit.test.ts` for the L2.8 edge rate-limit (`middleware.ts`): `identify` IP resolution (XFF-leftmost › x-real-ip › "unknown", route-scoped key, whitespace/empty-XFF fallbacks), token-bucket arithmetic (exactly `BUCKET_CAPACITY` requests in an instantaneous burst, the +1th denied, one token restored after exactly `RETRY_AFTER_SECONDS` and not a tick before, refill clamped at capacity, per-key isolation), and opportunistic eviction (entry strictly older than `EVICTION_WINDOW_MS` deleted, edge/recent entries not — proven via a recording store that counts `store.delete` because eviction and a long-gap refill are observationally identical through the return value alone, D-047). Injected store + explicit `now` (no real timers, no shared module Map); real `NextRequest` drives `identify`.
- [x] L7.2 Add `tests/healthcheck-route.test.ts` for `app/api/healthcheck/route.ts`: ok→200 / degraded·down→503, `mode` (fixture/live) inferred from `SF_API_KEY`, `latency_ms` present and numeric, `Cache-Control: no-store`. Drive `GET` with a real `NextRequest`; neutralise `SF_API_KEY` per-test for the deterministic fixture path (D-044 pattern); cover the SF-throw → `down` branch via an injected/failing client if the route allows, else document the limitation.
- [x] L7.3 Add `tests/faq-jsonld.test.ts` for `app/_components/faq.tsx`: `faqJsonLd()` emits a valid schema.org `FAQPage` (`@context`/`@type`/`mainEntity` of `Question`+`acceptedAnswer`), one entry per `FAQ_ITEMS`, and the structured `acceptedAnswer.text` equals the plain-text `text` of each item (the visible/structured parity D-040 makes structurally impossible — this pins it so a refactor can't reintroduce drift).
- [x] L7.4 Add `tests/preview-route.test.ts` for `app/preview/page.tsx`: server component returns the first ≤5 reviews, the total shown is `place.rating_count` (not a walk count, D-041), missing/blank `placeId` renders the error/empty state not a crash, and `metadata.robots` is `noindex`. Neutralise `SF_API_KEY` for the fixture path; assert against the Next 15 async-`searchParams` contract. Driven without react-dom: `PreviewPage` is awaited (Next 15 async `Promise<searchParams>`), the returned element tree is walked **structurally** to count `ReviewRow` elements by component-function name and the pure hookless sub-components are invoked to flatten visible text; the small (rating_count 12, 5 rows) and large (rating_count 609, 5 rows, 500-review fixture) fixtures pin the D-041 "total ≠ walk/fetch count" invariant from both sides (D-050).
- [x] L7.5 Add `tests/seo-routes.test.ts` for `app/robots.ts` + `app/sitemap.ts`: robots disallows only `/api/`, both honour `NEXT_PUBLIC_SITE_URL` with the documented fallback (D-035), and the sitemap enumerates exactly `publishedVariants()` (empty pre-L3.1b — the assertion that flips when L3.1b publishes the top-5, mirroring the seo-variants suite's pre-L3.1b note, D-044).

## Phase 8 — Render-contract hardening (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-052: with every standard Phase 0–7 leaf `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), the routine had no agent-doable standard leaf. Phase 6 covered the pure library modules; Phase 7 covered the five runtime route/middleware modules. The still-untested source surface is the **component/page composition layer** (the variant route, the shared tool form, the home page) plus the small `cn()` util, plus the one production refactor D-048/D-051 explicitly pre-authorised: a healthcheck client-injection seam. Same committed-not-run posture as Phase 6/7 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L8.1 Add `tests/variant-page.test.ts` for `app/(seo)/[variant]/page.tsx`: pin the L3.1a inert-until-L3.1b contract — `dynamicParams === false`, `generateStaticParams()` equals `publishedVariants()` mapped to `{variant:slug}` (empty pre-L3.1b — the assertion that flips at L3.1b, D-044), `generateMetadata` returns `{}` for a real-but-unpublished registry slug and a garbage slug, and `VariantPage` calls `notFound()` for **every** registry slug (a refactor past the `published` gate ships ten live pages and fails loudly). Uses the D-050 no-react-dom structural walk; the published-render path is documented as an L3.1b-era follow-up (unreachable pre-L3.1b without mutating the frozen registry or mocking — D-048 posture).
- [x] L8.2 Add `tests/review-tool-form.test.ts` for `app/_components/review-tool-form.tsx`: the form must be `<form action="/preview" method="GET">` (the D-041 rewire — guards against a regression back to the raw `/api/reviews` `target="_blank"` flow), carry exactly one **required** text input `name="placeId"`, and render the three format radios `name="format"` with values `json`/`csv`/`xlsx` and `json` `defaultChecked` (the order the form surfaces — same check-order spirit as L6.5/D-046). Structural walk per D-050 (it is a pure synchronous component, no async/searchParams). Pure structural walk (asserts on `el.type` strings — no sub-components to flatten, the simplest D-050-family suite); redundant negatives (`action` not `/api/reviews`, `target` undefined) pin the silent D-041-revert threat; format radios pinned by ordered value-array + exactly-one-`defaultChecked`=`json` (D-053). Same committed-not-run posture (D-039/D-040/D-042; D-050).
- [x] L8.3 Add `tests/home-page.test.ts` for `app/page.tsx`: composition contract — renders `<ReviewToolForm/>` and `<FaqSection/>`, the static `metadata` has the expected title/description, and the page contains **no** stale "ships in L2.5" placeholder text (D-041 removed it from both the home page and the variant route; pin that it stays gone). Structural walk + pure-component flatten per D-050.
- [x] L8.4 Add `tests/cn-util.test.ts` for `lib/utils.ts`: the `cn()` className helper — clsx conditional/array/object inputs are joined, falsy entries dropped, and the tailwind-merge contract holds (later conflicting Tailwind class wins, e.g. `cn("p-2","p-4") === "p-4"`; non-conflicting classes both survive). Tiny but pure and depended on by every component; locks the class-merge behaviour against a clsx/tailwind-merge bump.
- [x] L8.5 Add a route-level client-injection seam to `app/api/healthcheck/route.ts` (the production refactor D-048/D-051 explicitly named — *not* a test hack: it also unblocks an L4.1 live-path test) so `GET` accepts an optional injected SF client, then extend `tests/healthcheck-route.test.ts` to cover the previously env-unreachable `degraded` (client returns no `place`) and `getReviews()`-throws → `down` branches. Keep the no-mock posture: drive the new branches with a tiny hand-written stub client passed through the seam, not `vi.mock`. **Touches production code** — the only Phase 8 leaf that does; carry a design note that the seam is a reviewed, intentional change distinct from the test-hack D-048 rejected.

---

## Phase 9 — Layout & shell hardening (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-057: with every standard Phase 0–8 leaf `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), the routine had no agent-doable standard leaf. Phase 6 covered the pure library modules; Phase 7 the runtime route/middleware modules; Phase 8 the component/page composition layer. The last runtime module without a dedicated suite is `app/layout.tsx` — the document shell and the env-gated Plausible analytics snippet (L2.10). Same committed-not-run posture (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L9.1 Add `tests/root-layout.test.ts` for `app/layout.tsx`: pin the env-gated Plausible snippet (L2.10) from both sides — `RootLayout` renders the `next/script` `<Script>` **only** when `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set (correct `data-domain`/`src`/`defer`/`strategy: afterInteractive`) and renders **no** analytics script when it is unset (the privacy/opt-in regression both ways) — plus the document shell: a single `<html lang="en">`, a single `<body>` carrying the `bg-background`/`text-foreground`/`min-h-screen` tokens, and `children` threaded through into the body. Env captured/restored per-test (D-044); `<Script>` matched by reference identity against the same `next/script` import + a props cross-check (D-057); no-react-dom structural walk (D-050).

## Phase 10 — Fixture & data-contract hardening (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-058: with L9.1 merged (#54) every standard Phase 0–9 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and every **runtime module** now has a dedicated suite (Phase 6 the pure libs, 7 the routes/middleware, 8 the components/pages, 9 the layout). The one cross-cutting surface every other suite and the whole mock-first contract depends on — the committed `mocks/semanticforce/` fixtures themselves — is still unguarded: `lib/semanticforce/client.ts` imports each JSON `as unknown as Fixture`, so TypeScript validates nothing about their shape. Phase 10 pins the data those suites trust. Same committed-not-run posture (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L10.1 Add `tests/fixtures-contract.test.ts` for `mocks/semanticforce/{small,mid,large}-business.json` (validated through `client.__testing.FIXTURES`, the objects the runtime actually serves): registry shape (exactly the three `MOCK_*_001` keys; each fixture's `place.place_id` == its registry key, the `pickFixture` exact-match path); committed sizes 12/80/500 (pins the **committed reality** — the L1.3 prose says "small (10)" but the file is 12 reviews / `rating_count` 12, the number the preview suite already hard-codes per D-041, so a silent shrink to 10 would desync the two); `rating_count >= reviews.length` for every fixture (the **data-source side** of the D-041 "total shown is `rating_count`, never a walk/fetch count" invariant — a fixture whose count dropped below the array length would make the preview total silently under-report); the full `Review`/`PlaceMeta` per-record shape (unique non-empty `review_id`, integer `rating` in 1..5, non-empty `author_name`/`text`, round-tripping ISO-8601-UTC `published_at`, and well-formed optional `photos`/`owner_response`/`language`); and the L1.3-promised coverage actually surviving (the full 1..5 star spread across the corpus, ≥4 distinct languages, ≥1 photo + ≥1 owner response, and non-ASCII text present — the latter tying to the `feedback_csv_ascii_for_excel` unicode-survival memory). Suite count now 18 `tests/*.test.ts` files (the L9.1 prose's "18" ran one ahead of the real file total — root-layout was the 17th file; this is the 18th).

---

## Phase 11 — Suite-deepening tranche (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-059: with L10.1 merged (#55), every standard Phase 0–10 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and per D-058 every runtime module *and* the underlying fixture data has a dedicated suite. The next layer of agent-doable hardening is deepening the existing thin suites where a single load-bearing concern is still unguarded — L11.1 deepened the FAQ surface (visible-render half added alongside the JSON-LD half), L11.2 deepened api-reviews (userLimit slicing + slug-in-filename), L11.3 deepened place-id (surface-form aliasing + canonical-form idempotency + pattern minimum-length floor — the cache-key integrity engine, D-061), L11.4 deepened sf-client (FixtureClient page-boundary slicing + HttpClient URL hygiene + decodeCursor offset-field guards — the SF wire surface, D-062), L11.5 deepened seo-variants (slug-id pair freeze + SERP-length ceiling + topical-keyword presence — the URL/display/keyword silent-regression surfaces of the L3.1a long-tail registry, D-063), L11.6 deepens export-csv (CSV_COLUMNS exact-order freeze + per-row arity + embedded comma/CR/LF/quote survival + empty-reviews shape + single-photo boundary + filename slice ceiling — the user-facing data deliverable, D-064). Same committed-not-run posture (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L11.1 Add `tests/faq-section.test.ts` for `FaqSection()` in `app/_components/faq.tsx`: pin the visible-render half of the FAQ contract that `tests/faq-jsonld.test.ts` cannot reach — exactly one `<section aria-labelledby="faq-heading">` paired with exactly one `<h2 id="faq-heading">FAQ</h2>` (the silent-break pair: aria-labelledby loses its target if either side drifts, dissociating the visible heading from the region — a screen-reader regression no JSON-LD test would catch), exactly `FAQ_ITEMS.length` `<details>` entries in surfaced order (nth `<summary>` contains nth `item.q`, the L6.5/D-046 check-order spirit so visible accordion order can't desync from the JSON-LD `mainEntity` order), all default-closed (no stray `open` prop — a layout regression the design rejects), and crucially the visible answer body renders the **rich JSX `item.a`** (not the plain-text `item.text` the JSON-LD ships) — proven by the presence of a `<code>` element inside the rate-limits entry's `<details>` (only `item.a` carries `<code>` markup, never `item.text`; substring matches on "429" would still pass after a swap, structural existence fails loudly), with guard-the-guard that the visible flatten reads as real glyphs (no surviving `&apos;`/`&amp;`/`&quot;` — the JSX runtime converts entities before they leave the element, so any survivor means a vacuous walk or naive stringification). Same D-050 no-react-dom structural walk as review-tool-form / variant-page / root-layout.
- [x] L11.2 Deepen `tests/api-reviews.test.ts` with the two unguarded load-bearing concerns the route's HTTP-contract suite was missing: **(a) `userLimit` response slicing for all three formats** — `?limit=3` on the 12-review small fixture must produce JSON `reviews.length === 3`, CSV with exactly 4 non-empty CRLF lines (header + 3 data), XLSX with exactly 4 cell rows (asserted via `XLSX.read` → `sheet_to_json({ header: 1 })`); plus boundary cases `?limit=99999` past the fixture returns all 12 not garbage (the `Array.slice` past-the-end safety the route inherits) and `?limit=3.7` floors to 3 (the route's documented `Math.floor` intent), asserted from the *response* not the route helper so a refactor moving the slice elsewhere still has to satisfy it. **(b) The slug-in-filename contract** — `Content-Disposition` for csv/xlsx must use the normalised slug (lowercase, dash-separated) not the raw input form: pinned with a mixed-case underscore input `mock_SMALL_001` so the slug transform surfaces, asserting `filename="google-reviews-mock-small-001-\d{8}\.(csv|xlsx)"` *and* the negative that the underscore form `mock_small_001` does not appear (substring on "mock" alone passes on the raw form too — the dashed/lowercased form is the load-bearing assertion). Suite grows from 7 it-blocks/3 describes to 14 it-blocks/5 describes. The X-Cache: HIT path is deliberately deferred (unreachable through the public route surface in fixture mode without an injection seam or a memory-mode singleton; either is a production change heavier than this leaf needs — D-060). Same committed-not-run posture (D-039/D-040/D-042).
- [x] L11.4 Deepen `tests/sf-client.test.ts` — the SemanticForce client suite — with three cross-cutting load-bearing concerns the existing coverage missed (D-062). **(a) FixtureClient page-boundary slicing.** The existing 500-review LARGE-fixture walk satisfies "every id seen exactly once" via set-size, but 500 is exactly divisible by 100, so a regression that emitted a spurious tail cursor (off-by-one `<=`) or swallowed a partial last page (off-by-one `>`) would still satisfy the LARGE walk and ship silently. Pin on the SMALL fixture (12 reviews) where boundaries surface in two passes: limit=5 → pages of 5+cursor, 5+cursor (cursor must **advance** — same cursor twice would infinite-loop any walker), 2+**no cursor** (the partial-tail "we're done" signal); limit=12 exactly → one page + **no cursor** (the exact-fit "no spurious extra RTT" signal, the off-by-one switch from both directions). **(b) HttpClient URL hygiene — what the wire must NOT contain.** Two silent-SF-rejection regressions with no test signal otherwise (we'd see an opaque 4xx from SF in L4.1): user-supplied `limit: 9999` must serialise as `limit=100` on the wire (clamped via `clampLimit`, not raw — a refactor that skipped the clamp would have SF reject the request as out-of-spec); when no `after` is supplied the URL must not contain an `after` parameter at all (asserted belt-and-braces with both `searchParams.has("after") === false` and `toString().not.toContain("after=")` so a refactor that set `after=undefined` always — which serialises to the literal string `"undefined"` — fails on both checks). **(c) `decodeCursor` offset-field guards.** The existing helper test pins the outer try/catch (garbage base64 → 0, missing → 0); the inner `typeof parsed.offset === "number" && parsed.offset >= 0` guard was unpinned, so a refactor that loosened it to `parsed.offset ?? 0` would let `-5` through and `Array.slice(-5, …)` silently slices "from the end" — a fixture-page corruption with no error signal. Pin `{offset: -5}`, `{offset: "12"}`, `{offset: null}`, `{}` all → 0, plus a positive `{offset: 7}` → 7 round-trip so the negatives can't pass vacuously on a broken decoder (the L11.3-style guard-the-guard). Suite grows from 13 it-blocks/3 describes to 18 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).
- [x] L11.3 Deepen `tests/place-id.test.ts` — the canonicalisation engine that decides cache keys (D-020 `gr:reviews:v1:<slug>`), share-URL segments, and download filenames (D-033) — with the three cross-cutting load-bearing concerns the single-rule suite never reached. **(a) Surface-form aliasing → identical `{raw, slug}` (the cache-key integrity contract)** — the canonical `REAL_ID` (`ChIJN1t_tDeu…`) and four equivalent surface forms (lower-prefix `chij…`, whitespaced `\t  …\n`, embedded in a long Google Maps URL) all `.toEqual()` one canonical pair, plus separate aliasing inside the MOCK_ family (upper/lower/mixed → one pair) and the 0x family (upper-hex vs lower-hex → one pair); a regression that fragmented the KV cache into one entry per surface form (or changed the download filename between visits) fails loudly. **(b) Canonical-form idempotency** — `normalisePlaceId(normalisePlaceId(x).raw)` returns the same `{raw, slug}` as `normalisePlaceId(x)`, started from a non-canonical input (lower-prefix `chij…`) so the second pass actually exercises the canonicalisation arrow rather than passing vacuously; extended over MOCK_ and 0x families. Pins the cache write→read fixed-point property: a normalise → store → re-normalise → fetch loop must not silently miss its own writes. **(c) PLACE_ID_PATTERN minimum-length floor** — `ChIJ[\w-]{20,}` rejected at 19 tail chars and accepted at exactly 20 (both ends of the boundary so a future `{19,}`/`{21,}` flip fails loudly), `0x:0xABCD` and `0xABCD:0x` (empty-hex sides) rejected, bare `MOCK_` (no body) rejected — a refactor that loosened `{20,}` to `{1,}` or `+` to `*` would otherwise let almost-real stubs reach the live SF call as confusing upstream 404s instead of a red test at our edge. Suite grows from 13 it-blocks/3 describes to 22 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).
- [x] L11.6 Deepen `tests/export-csv.test.ts` — the CSV writer suite that already proves BOM/CRLF terminators, header presence + row count, QUOTE_ALL with embedded `"`/`\n`, unicode survival, the photo-URL multi-join via `__testing.rowFor`, and `csvFilename`'s data-vintage date — with the three cross-cutting load-bearing concerns the existing 7-it/3-describe suite never reached (D-064). **(a) `CSV_COLUMNS` exact-order freeze + per-row arity.** Downstream pipelines read the CSV by column position; a refactor that silently swapped `rating` ↔ `language` (or inserted a column in the middle) would shift every following cell across every consumer with no runtime signal. Pin the exact 14-element ordered tuple, pin the header row parses to that same tuple in surfaced order (the writer's header-emission path), and pin **every** emitted row carries exactly `CSV_COLUMNS.length` cells via parser round-trip — a row whose cell count drifts misaligns loudly. **(b) Embedded CSV-danger characters survive QUOTE_ALL escaping — all four, not just two.** The existing suite proves embedded `\n` + `"` survive in `text`; embedded **comma** (the field delimiter — a refactor that dropped quoting entirely would split a row on a single user-typed comma) and embedded **CR** (half of the row delimiter — a naive splitter would mid-row terminate) were unguarded. Pin one review whose `text` carries all four chars and assert parser round-trip yields the original intact, row count stays at header + 1 (embedded CR/LF didn't split), and cell count stays at `CSV_COLUMNS.length` (embedded comma didn't shift). Plus a comma-only follow-up pinning downstream `rating`/`published_at` by value so a column shift fails at a known cell, not vacuously at a substring match. **(c) Empty reviews shape + single-photo `photo_count` boundary + filename slice ceiling.** (i) Empty `payload.reviews` still emits BOM + header + trailing CRLF (a brand-new place must still produce a valid Excel file). (ii) The 1-photo case pins `photo_count='1'` and `photo_urls` as the bare URL with no trailing PHOTO_URL_JOIN separator (the existing 2-photo case can't distinguish a `url1 | ` regression). (iii) `csvFilename` slices the first 10 chars of `dateIso` — a refactor to `slice(0, 8)` or `slice(0, 11)` would misname every download; pin the slice ceiling at a year/month/day boundary so either drift fails loudly. Suite grows from 7 it-blocks/3 describes to 15 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Suite-local minimal RFC-4180 parser handles the QUOTE_ALL output; not shipped. Same committed-not-run posture (D-039/D-040/D-042).
- [x] L11.5 Deepen `tests/seo-variants.test.ts` — the L3.1a registry that feeds `generateStaticParams`/sitemap/the dynamic route — with the three cross-cutting load-bearing concerns the existing shape-and-publish-gate suite never reached. The three silent-regression surfaces of a long-tail SEO registry are the URL, the SERP display copy, and the target keyword; this leaf pins each. **(a) Slug-id pair freeze (URL contract).** The `{id, slug}` mapping is both the `docs/seo-variants.md` cross-reference and the canonical URL of each variant once L3.1b publishes it; a regression that silently renamed a slug (`export-google-reviews-to-csv` → `export-google-reviews-csv`) changes the page's permanent URL forever — every inbound link 404s and the L1.6b/corgi keyword score that picked it becomes meaningless. Pin the exact 10 `[id, slug]` pairs in registry order so any change is a reviewed change. Paired with a group balance count (4 A-prefix + 3 B-prefix + 3 C-prefix ids — the `docs/seo-variants.md` grouping that "10 with unique ids" alone wouldn't catch if a Group A entry were silently swapped for a Group B). **(b) SERP-length ceiling on metaTitle/metaDescription.** Google truncates `<title>` at ~60 chars and `<meta description>` at ~160 chars in the SERP display (Moz/Ahrefs conservative thresholds); a refactor that bloated a title past 60 silently lops off the tail — the page still ships, the keyword still ranks, but CTR drops with no runtime signal. Pin the ceiling at 60/160 with non-empty floors, plus a guard-the-guard "longest current title fits" assertion so the ceiling can't pass vacuously on an empty registry or a tabulation that dropped string content (the L11.3-style positive-round-trip pattern). **(c) Topical-keyword presence on the three SERP-weighted surfaces.** The whole reason these variants exist is the "google reviews" long-tail keyword cluster (L1.6a); a refactor that templated the strings (e.g. `s/Google Reviews/customer feedback/g`) would silently turn every page into a topical mismatch for its target query, and the registry-shape block would still pass (strings still non-empty and URL-safe). Pin "google" AND "review" presence (case-insensitive) on `metaTitle` AND `h1` AND `metaDescription` — the three surfaces a search engine actually weights, asserted independently so a partial regression on any one fails loudly. The `intro` paragraphs are deliberately *not* asserted: B3's `intro[0]` legitimately omits "google" (the body copy is allowed to vary as long as the SERP-visible surfaces lock the keyword). Suite grows from 5 it-blocks/2 describes to 13 it-blocks/5 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 12 — Suite-deepening tranche, continued (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-064/D-065: with L11.6 merged (#60), every standard Phase 0–11 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and Phase 11 deepened six in-flight load-bearing surfaces (FAQ visible-render, api-reviews HTTP, place-id canonicalisation, sf-client paging/wire/cursor, seo-variants URL/SERP/keyword, export-csv writer). Several runtime-module suites that landed in a single first-pass tranche (Phase 6–10) still carry a single load-bearing concern unguarded — Phase 12 walks those, starting with the XLSX writer (L12.1, the natural pair to the L11.6/D-064 CSV writer), then the KV cache (L12.2, the wire surface between us and the SF spend-cap — TTL boundary direction, KvRestCache `get` error-class fan-in + `set` best-effort swallow, and `get`/`set` REST-pipeline header symmetry). Same committed-not-run posture (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L12.1 Deepen `tests/export-xlsx.test.ts` — the XLSX writer suite that already proves sheet shape (one "Reviews" sheet, header + N data rows, 14-column `!ref`), the frozen header pane (`ySplit:1` via either `!views` or legacy `!freeze`, the D-043 tolerant freeze guard), hand-tuned `COLUMN_WIDTHS`, `__testing.rowFor`'s photo-join + blanks-for-missing-optionals, and `xlsxFilename`'s data-vintage YYYYMMDD — with the three cross-cutting load-bearing concerns the existing 4-describe/8-it suite never reached. **(a) `XLSX_COLUMNS` exact-order freeze + referential identity with `CSV_COLUMNS` + per-row arity via `XLSX.read`.** Pin the exact 14-element tuple (a length check or `Object.keys(rows[0]).toEqual([...XLSX_COLUMNS])` echo isn't enough — the latter is a tautology since the array drives the read-back keys; pin the literal tuple directly). Pin `XLSX_COLUMNS === CSV_COLUMNS` (reference equality, not `.toEqual`) so the documented single-source-of-truth invariant (`export const XLSX_COLUMNS = CSV_COLUMNS`) holds structurally — a refactor that switched to `[...CSV_COLUMNS]` (a copy) would pass `toEqual` today but let the two arrays drift silently on the next column edit. Pin **every** row carrying exactly `XLSX_COLUMNS.length` cells via `sheet_to_json({ header: 1 })` (array-of-arrays form, not key-merged) — a row whose cell count drifted from 14 surfaces as a length mismatch rather than being papered over by the read-back's header-merged shape. **(b) Typed-cell fidelity — numeric vs string cells survive the round-trip.** Unlike CSV (all fields text), XLSX cells carry types: `rating` and `photo_count` are JS numbers in the row object, and SheetJS writes them as numeric ("n") cells; round-trip preserves `typeof === "number"`. A refactor that stringified via `String(r.rating)` or `.toString()` breaks `=SUM`, `=AVERAGE`, numeric sort on the rating/count columns with no visible error in the file. Pin `typeof rows[0].rating === "number"` and `=== 5`, `typeof rows[1].rating === "number"` and `=== 3`, plus `photo_count` typed-number from both the 2-photo and 0-photo sides (the no-photos case must be `0`, not `""` — the `rowFor` blank-fill is for string fields only). Counterpart string assertions on `review_id`/`published_at` so an opposite regression (coercing an all-digit id to number) also fails loudly. **(c) Empty-reviews shape + single-photo boundary + filename slice ceiling.** (i) Empty `payload.reviews` still emits a valid workbook: one sheet named "Reviews", header-only grid via `sheet_to_json({ header:1 })` (length 1, equals `[...XLSX_COLUMNS]`), `!cols` still 14 entries, freeze pane still `ySplit:1` — a brand-new place with zero reviews must still produce a usable file. (ii) Single-photo case: `photo_count === 1` (typed number) and `photo_urls === "https://p/only.jpg"` with `not.toContain(PHOTO_URL_JOIN)` — the existing 2-photo `url1 | url2` assertion can't distinguish a `photos.map(p => p.url + PHOTO_URL_JOIN).join("")` regression that emits `url1 | `, the 1-photo case does. (iii) `xlsxFilename` slices the first 10 chars of `dateIso` — pin at year/month/day boundaries (`2026-12-31T…` → `20261231`, `2026-01-01T…` → `20260101`) so a refactor to `slice(0, 8)` (drops day) or `slice(0, 11)` (lets the trailing `T` survive into the filename) fails loudly. Symmetric with the L11.6/D-064 `csvFilename` pin. Suite grows from 4 describes/8 it-blocks to 7 describes/17 it-blocks; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

- [x] L12.3 Deepen `tests/cn-util.test.ts` — the `cn()` className helper every component calls to compose classes (D-068). The existing 11 it-blocks/2 describes prove clsx input-handling (string/array/object/conditional, falsy drops, empty result `""`) and the tailwind-merge same-class last-wins rule (`cn("p-2","p-4")==="p-4"`); three cross-cutting load-bearing concerns the trivial same-class tests cannot reach were unguarded. **(a) Directional vs shorthand interaction.** The `cn(base, overrides)` component pattern is exactly `cn("p-4", "px-2")` → "p-4 px-2" (later more-specific keeps both, broad survives for non-overridden axes); reversed `cn("px-2", "p-4")` → "p-4" (later broader nukes specific). A regression to naive same-prefix last-wins passes every existing test and silently breaks every component using the override pattern. Pinned in four directions including the multi-step collapse `cn("pt-2", "pl-4", "p-8")` → "p-8". **(b) Variant-aware conflict scoping.** `hover:`/`sm:` open independent merge buckets, so `cn("p-2", "hover:p-4")` keeps both but `cn("hover:p-2", "hover:p-4")` collapses to `hover:p-4`. Pinned across two distinct variants (so a partial regression on one fails), plus a three-bucket case (`"p-2 sm:p-4 sm:p-8 hover:p-4"` → `"p-2 sm:p-8 hover:p-4"`) proving the bucketing is real, not coincidental. **(c) Property-aware (not prefix-aware) conflicts.** `text-sm` (font-size) and `text-blue-500` (color) share prefix `text-` but target distinct CSS properties — both must survive; `text-red-500` + `text-blue-500` collapses (same property, color); `font-bold` + `font-medium` collapses (second property family so color can't pass vacuously); `bg-blue-500` + `text-white` keeps both (wholly distinct). Plus the zero-arg edge `cn()` → `""` and the pre-joined-string edge `cn("p-2 p-4")` → `"p-4"` (whitespace tokenised inside a single arg, not just across args). Suite grows from 11 it-blocks/2 describes to 24 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

- [x] L12.2 Deepen `tests/reviews-cache.test.ts` — the cache module that decides every `/api/reviews` hit/miss path and the only KV-REST wire-format guard the repo carries — with the three cross-cutting load-bearing concerns the existing 9-it/3-describe suite never reached. The Phase 12 pattern (L12.1/D-065): suites that already prove "the happy path works" need the silent-regression edges pinned. **(a) TTL boundary direction + absolute `CACHE_TTL_SECONDS` freeze.** The existing `MemoryCache` test pins `TTL−1` → hit and `TTL+1` → miss, leaving the exact `expires_at` tick unguarded — `MemoryCache.get` uses `this.now() > entry.expires_at` (strict `>`), so the boundary tick must be a **hit**, and `expires_at + 1` must be the first **miss**. A refactor flipping to `>=` would expire entries one ms early on every call (a fixed 1-ms TTL truncation on every cache write, invisible until the rare overlap of `now()` and `expires_at`); pinning both sides of the boundary fails loudly. Pair with `expect(CACHE_TTL_SECONDS).toBe(24 * 60 * 60)` and `expect(CACHE_TTL_SECONDS).toBe(86400)` (both forms — the named arithmetic catches a swap to `12 * 60 * 60`, the literal catches a swap to `24 * 60` that the named form would also pass) so a refactor to "12h" or "24m" fails at our edge instead of silently doubling our SF API spend. Methodology §3 documents 24h; pin the constant the docs name. **(b) `KvRestCache.get` error-class fan-in + `set` best-effort swallow.** The existing `get` suite covers `result:null` → miss and `fetchImpl`-throw → miss; the four other documented error-classes the implementation explicitly catches are unguarded — a refactor that narrowed any `catch` to a specific error type (or dropped one entirely) would let a transient KV outage crash `/api/reviews` instead of degrading to a miss. Pin all four end-to-end on `.get()`: (i) non-2xx response (`new Response("", { status: 502 })`) → `null`; (ii) 200 with malformed JSON body (`new Response("not json", { status: 200, headers: { 'Content-Type': 'application/json' } })`) → `null` (the inner `await res.json()` catch); (iii) 200 with valid envelope but unparseable `result` string (`{ result: "not json" }`) → `null` (the inner `JSON.parse(body.result)` catch); (iv) 200 with `result: undefined` → `null` (the `body.result == null` branch, distinct from explicit `null`). Plus the symmetric `set` best-effort contract that has **no** test today: `await cache.set(...)` must **not reject** when `fetchImpl` throws — a stub that throws synchronously, asserted via `await expect(cache.set("slug", payload())).resolves.toBeUndefined()`. The route shape (`route.ts` calls `await cache.set(...)` after the SF walk; an unhandled rejection would 500 the response after the user already paid for the SF API call) means this is the one silent failure mode that costs both UX and money. **(c) `get` REST-pipeline symmetry with `set` + trailing-slash strip on both paths.** The existing `set` test pins URL trailing-slash strip (`"https://kv.example/"` → `"https://kv.example"`), Bearer-auth header, and `Content-Type: application/json`; the `get` test asserts only the request body shape and leaves URL/header symmetry unpinned. A refactor that broke either on the `get` path (e.g. moved `headers()` out of `KvRestCache.get` while keeping it in `set`) would still pass the `set` suite *and* the `get` body-shape suite — silent until L4.1 hit a live KV that rejected the malformed `get`. Pin on `.get()`: trailing-slash-stripped URL (the same `"https://kv.example/"` → `"https://kv.example"` assertion already asserted for `set`), `Authorization: Bearer tok` header, and `Content-Type: application/json` header. Same `RecordedCall` stub; one new `it` proves the three properties symmetrically. Suite grows from 9 it-blocks/3 describes to 17 it-blocks/4 describes (or 5 if the boundary tests get their own `describe`); suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 13 — Suite-deepening tranche, route-handler half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-068/D-069: with L12.3 merged (#65), every standard Phase 0–12 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and Phase 12 deepened three more in-flight load-bearing surfaces (XLSX writer, KV cache, `cn()` className merger). Several Phase 7 route/middleware suites still test only the **internal helpers** their `__testing` blocks expose — never the public default-export the runtime actually calls — leaving the HTTP-response contract (status, headers, body envelope, route matcher) unguarded behind a green internals-only suite. Phase 13 walks those, starting with the abuse-surface gate (L13.1 — `middleware()` end-to-end on top of the existing `consumeToken`/`identify` internals; pins `config.matcher` exact scope, the 429 + `Retry-After: 6` + `{error:{code,message}}` envelope symmetric with `SemanticForceError` D-027, and the `NextResponse.next()` pass-through on allow). Same committed-not-run posture (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L13.2 Deepen `tests/healthcheck-route.test.ts` — the SF liveness-probe suite that already pins ok/down status mapping, mode-from-`SF_API_KEY`, `Cache-Control: no-store`, and the L8.5 client-injection seam — with the four cross-cutting load-bearing concerns the existing **internals + seam** suite never reached, in the Phase 13/D-069 "public-export end-to-end" pattern. **(a) `runtime = "edge"` exact-equals pin** as its own one-`it` describe (the route-config analogue of L13.1's `config.matcher` pin — a refactor that dropped the named export silently moves the probe to the Node.js runtime, different cold-start + fetch semantics, no helper or seam test catches this). **(b) Response-envelope structural shape via `Object.keys(body).sort()` exact equality** on both ok (five keys: `checked_at`/`latency_ms`/`mode`/`place_id`/`status`, no `error`) and down (six keys including `error`), plus `Object.keys(body.error).sort()` exactly `["code","message"]` — symmetric with L13.1/D-069's middleware envelope assertion (a refactor that added a surplus key, renamed `error.code` to `error_code`, or returned `{error: "down"}` (string form) still 503-s and still emits status/mode but silently breaks every uptime monitor or UI consumer that destructures `body.error.code`; D-027 envelope contract). **(c) `Content-Type: application/json` header pinned on both ok and down paths** (a refactor that switched from `NextResponse.json` to a text response or dropped the header would still pass every status/envelope test but break `await res.json()` in every monitor). **(d) `latency_ms` is a real measurement — `Date.now() - startedAt` direction + non-constant** via `vi.spyOn(Date, "now").mockReturnValueOnce(start).mockReturnValueOnce(end)` (the two Date.now calls in `handle()` — `new Date()` for `checked_at` uses a separate internal time source and is not intercepted by the spy), with distinct integer deltas on the ok path (500) and the throw path (750) so a refactor that hard-coded `latency_ms: 500` passes ok and fails down — pinning the field is *computed*, not a constant. The existing `>= 0` + `Number.isFinite` checks survive as the real-clock guard; the spy tests pin the arithmetic direction at an exact integer instead of a flaky real-time window. Suite grows from 12 it-blocks/4 describes to 19 it-blocks/8 describes; suite-file count unchanged at 19 (deepening only, no new file). `vi`/`afterEach`-restore added to the existing `vitest` import; `runtime` imported as a named export from `@/app/api/healthcheck/route` (already top-level). Same committed-not-run posture (D-039/D-040/D-042).
- [x] L13.1 Deepen `tests/middleware-ratelimit.test.ts` — the edge rate-limit suite that already pins constants, `identify` (XFF leftmost / trim / x-real-ip fallback / unknown / route-scoped key), `consumeToken` arithmetic (burst, fresh-key, retry-window edge, cap, separate keys), and opportunistic eviction with a recording store — with the three cross-cutting load-bearing concerns the existing **internals-only** suite never reached: every assertion today destructures `__testing` and drives helpers in isolation; the public default `middleware()` export the Next runtime actually invokes is exercised nowhere. **(a) `config.matcher` exact-scope freeze.** Pin `expect(config.matcher).toEqual(["/api/reviews"])` as its own one-`it` describe. The matcher is the route-scoping the limiter relies on at runtime; a refactor that broadened to `["/api/(.*)"]` would silently start metering `/api/healthcheck` (every oncall poll burns a token — the L4.1 healthcheck-pinger regression with no test signal), a narrowing to `["/api/preview"]` or `[]` would silently leave the abuse surface unmetered, and a string-vs-array change (`matcher: "/api/reviews"`) is valid Next config but breaks the documented "array of matchers" shape this suite documents. The internals-only suite cannot catch any of these — the helpers pass identically regardless of how the route handler is wired. **(b) Denial-path end-to-end response contract on `middleware()`, not on `consumeToken`'s `false`.** Drive the public function with a real `NextRequest("https://example.com/api/reviews", { headers: { "x-real-ip": "192.0.2.7" }})`, freeze `Date.now()` via `vi.spyOn(Date, "now").mockReturnValue(<fixed>)` and clear `__testing.buckets` per test (the module-level Map the public function reads — the only state the public path consumes from the internals-only suite's universe), then assert the `(BUCKET_CAPACITY+1)`th call returns **`status === 429`** AND **`headers.get("Retry-After") === "6"`** (the literal string-form `String(RETRY_AFTER_SECONDS)` the code emits — both forms asserted, the named-constant form catches a typo to `RETRY_AFTER_SECONDS`'s value, the literal `"6"` catches a refactor that flipped `RETRY_AFTER_SECONDS` to a different number while leaving the header serialization intact) AND the JSON body is **structurally** `{ error: { code: "rate_limited", message: <non-empty string> }}` with `Object.keys(body)` exactly `["error"]` and `Object.keys(body.error).sort()` exactly `["code","message"]` — the load-bearing envelope shape symmetric with `SemanticForceError` (D-027: the UI handles upstream-rate-limited and our-rate-limited identically). A refactor that dropped the wrapper key (`{ code, message }`), renamed `code` to `error_code`, or returned `{ error: "rate_limited" }` (string form) would still pass every internals test, would still 429-the-user, and would silently break every UI consumer that destructures `body.error.code` — exactly the spirit of the D-051 / L11.6 "the *shape* of what we ship to the user is the contract, not the helper that produces it" pattern. **(c) Allow-path pass-through is not 429 and carries no `Retry-After`.** A single pre-drain call with a fresh bucket asserts `res.status !== 429` and `res.headers.get("Retry-After") === null` — the contract under test is "the pass-through path does not look like a denial", not the exact `NextResponse.next()` sentinel header (`x-middleware-next: 1`) which is a Next-runtime internal that has moved across minor versions and is the D-043-style tolerant-pin anti-pattern to assert directly. Without this, a refactor that replaced `NextResponse.next()` with `NextResponse.json(null)` (silently returning an empty body for every successful request) passes the internals suite, passes the matcher pin, passes the denial-envelope pin, and silently breaks every `/api/reviews` consumer downstream of the middleware — but fails this assertion because the empty-JSON response would still not carry `Retry-After` AND the matcher-broadening regression in (a) would let this assertion catch a stray 429 on /api/healthcheck if the matcher widened and the bucket was full. Same Date.now freeze + `buckets.clear()` per test so the allow-path test cannot accidentally consume tokens drained by the denial-path test (or vice versa); the existing internals-only tests use injected stores and are wholly unaffected. Suite grows from 11 it-blocks/4 describes to ~15 it-blocks/7 describes (matcher: 1 it / 1 describe, denial envelope: 2 it / 1 describe, allow pass-through: 1 it / 1 describe); suite-file count unchanged at 19 (deepening only, no new file). `vi`/`beforeEach`/`afterEach` added to the existing `vitest` import; `middleware` + `config` imported as named exports from `@/middleware` (both already top-level). Same committed-not-run posture (D-039/D-040/D-042).

## Phase 14 — Suite-deepening tranche, SEO/freshness half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-070/D-071: with L13.2 merged (#67), every standard Phase 0–13 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2), and Phase 13 deepened the two route-handler suites whose `__testing` blocks left the public default export unguarded (middleware, healthcheck). The next un-deepened public-export route surface — `app/robots.ts` + `app/sitemap.ts` — was explicitly named in D-070's closing line: each module carries its own *private* `siteUrl()` helper (same `FALLBACK_SITE_URL` constant, same `?.trim()` + `endsWith("/")` + `slice(0, -1)` chain, same blank→fallback path), and the existing suite tests robots fully across the four env states but tests only one of those four on the sitemap side, leaving cross-module symmetry — the property that robots `host` always equals every sitemap URL's origin — completely unpinned; the `lastModified` freshness contract (called out by name in D-070) is also unguarded. Same committed-not-run posture as Phase 11–13 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L14.1 Deepen `tests/seo-routes.test.ts` — the SEO crawl-policy + URL-enumeration suite that already pins robots' wildcard rule (allow `/`, disallow only `/api/`, never `/`) and four env states on the robots-`host` side, plus sitemap's root + `publishedVariants()` enumeration and pre-L3.1b root-only shape — with the three cross-cutting load-bearing concerns the existing 10-it/2-describe suite never reached, in the Phase 13/D-070 "two-private-copies-of-the-same-helper-must-not-drift" pattern (D-071). **(a) Cross-module `siteUrl()` symmetry — robots vs sitemap on all four env states.** `app/robots.ts` and `app/sitemap.ts` each carry a *private* copy of `siteUrl()` (same `FALLBACK_SITE_URL` constant, same `?.trim()` + `endsWith("/")` + `slice(0, -1)` chain, same blank→fallback path); the existing robots-side suite pins all four env states (unset, trailing-slash strip, whitespace trim, blank-fallback) but the existing sitemap-side suite pins only one (trailing-slash on root URL). A drift in one copy — a copy-edit to `FALLBACK_SITE_URL` in `robots.ts` only, a refactor that adds trim-and-not-strip in `sitemap.ts` — would silently make robots' `host` directive disagree with every sitemap URL's origin (Google relies on the cross-reference to identify the canonical host; a mismatch silently breaks canonicalisation). Pinned via a table-driven describe asserting `robots().host === sitemap-root-origin === expected` on all four env states symmetrically (robots' `sitemap` URL also pinned at `${expected}/sitemap.xml` so a drift orphans neither half). **(b) `lastModified` freshness — `Date` instance + computed-per-call, not hardcoded, not memoised at module load.** The sitemap suite asserts entry shape (url/changeFrequency/priority) but never the freshness contract; `lastModified` is what makes Google's `<lastmod>` track actual deploys. A regression to `new Date("2025-01-01")` (a literal vintage that ships with the code) or to a module-level `const now = new Date()` (memoised at import, frozen across calls) silently stops refreshing — Google would see a permanent stale lastmod and crawl less often, the page rank decays, with no runtime signal. Pinned both ways: (i) the field is a real `Date` instance whose `.getTime()` falls inside the call window (`before = Date.now()` ≤ `ts` ≤ `after = Date.now()` around the call) — proves it's computed inside `sitemap()`, not before; (ii) two consecutive calls return reference-unequal Date objects (`first !== second`) — proves `new Date()` per call, since the module-level constant regression would return the same instance forever even if timestamps coincide. **(c) URL uniqueness across all sitemap entries.** The existing sitemap suite counts entries by length and looks each variant URL up with `urls.has(...)`, but Set membership is silent about duplicates — a regression that double-spread `publishedVariants()` would satisfy `length === 1 + pub.length` only when `pub` is empty (it is pre-L3.1b), and once L3.1b lands the existing suite would silently accept a sitemap with duplicate variant URLs (splitting Google's link equity across one canonical + one duplicate, halving the analytics signal). Pinned as `new Set(urls).size === urls.length` so the load-bearing assertion fires at the L3.1b flip, trivially passes pre-L3.1b. Suite grows from 10 it-blocks/2 describes to 17 it-blocks/5 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

## Phase 15 — Suite-deepening tranche, third-route public-export half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-071/D-072: with L14.1 merged (#68), every standard Phase 0–14 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). Phase 13 deepened the two route-handler suites whose `__testing` blocks left the public default export unguarded (middleware L13.1, healthcheck L13.2); the same internals-and-helpers-vs-public-default-export gap remains on the third and largest route surface — `/api/reviews` — whose existing api-reviews suite asserts param-validation 400s, success Content-Type/Disposition, `userLimit` slicing (L11.2), and the slug-filename pipeline, but never the `runtime` named export, never the error-envelope structural shape (`Object.keys(body.error).sort() === ["code","message"]`), never the success-envelope keys, and never the `X-Cache` header symmetry across csv/xlsx. Phase 15 walks that gap with one consolidated leaf. Same committed-not-run posture as Phase 11–14 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L15.1 Deepen `tests/api-reviews.test.ts` — the `/api/reviews` HTTP-contract suite that already pins param-validation 400s (with check-order), success Content-Type/Disposition for csv/xlsx, case-insensitive `format`, `userLimit` slicing across all three formats (L11.2/D-060), the slug-in-filename pipeline (L11.2), and the `__testing` helper surface (`statusForCode`/`inferRetryAfter`) — with the three cross-cutting load-bearing concerns the existing 19-it/5-describe suite never reached, in the Phase 13/D-069/D-070 "public-export end-to-end" pattern (D-072). The route is the third public-export route surface; L13.1 deepened middleware, L13.2 deepened healthcheck, and the same internals-and-helpers-vs-public-default-export gap exists here: `runtime`, the error-envelope shape, and the success-envelope+header symmetry are nowhere asserted today. **(a) `runtime = "edge"` exact-equals pin** as its own one-`it` describe (the route-config analogue of L13.1's `config.matcher` and L13.2's `runtime` — a refactor that dropped the named export silently moves the route to the Node.js runtime, different cold-start + fetch semantics + CPU/memory tier on Vercel, and no in-process test of the response body/headers catches it because the route would still return identical content from Node). **(b) Error envelope structural shape via `Object.keys(body).sort()` exact equality** across the three 400 paths the suite already exercises (missing-placeId, bad-format, bad-limit): top-level keys exactly `["error"]`, and `Object.keys(body.error).sort()` exactly `["code","message"]` — symmetric with L13.1/D-069's middleware envelope and L13.2/D-070's healthcheck envelope (a refactor that added a surplus `details` field, renamed `code` to `error_code`, or returned `{error: "bad_request"}` (string form, dropping the wrapper) would still 400-the-user and still pass every existing `body.error.code` / `.message` check — silent UX regression for everything destructuring `body.error.code`, the same D-027 envelope-contract pattern). **(c) JSON success-envelope structural shape + `Content-Type: application/json` + `X-Cache: MISS` symmetry across csv/xlsx.** Three sub-pins on the success path: (i) JSON success body's `Object.keys(body).sort()` is exactly `["fetched_at","place","reviews"]` on the small fixture (which never trips HARD_CAP, so the optional `truncated:true` is absent and pinned-absent — a refactor that always-emits `truncated:false` or leaked `partial:[]` from the rate-limit branch or added a debug `meta` key fails loudly); (ii) the json success Content-Type is `application/json` (a refactor to `new Response(JSON.stringify(...))` silently drops the header — `await res.json()` still works, strict middleboxes/CDNs break); (iii) the csv and xlsx paths both carry `X-Cache: MISS` (the route emits it in all three branches of `respondSuccess`, but only the json path asserts it today — a regression that consolidated headers and dropped one branch silently breaks per-format cache-hit-rate analytics). The HIT path remains deferred per D-060 (unreachable through the public route surface in fixture mode without an injection seam or a memory-mode singleton — both heavier production changes than this leaf needs). Suite grows from 19 it-blocks/5 describes to 26 it-blocks/8 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

## Phase 16 — Suite-deepening tranche, preview-route metadata + CTA half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-072/D-073: with L15.1 merged (#69), every standard Phase 0–15 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). D-072's closing line named `tests/preview-route.test.ts` as the next agent-doable deepening candidate (the result-preview server component `app/preview/page.tsx`). Re-reading the suite: `metadata.robots` is already `toEqual({ index: false })` (an exact pin, not the loose substring D-072 mis-characterised) — the genuine load-bearing concerns the existing 8-it/4-describe suite leaves unguarded are (i) the **surplus-keys** threat on `metadata` as a whole (a refactor adding `openGraph`/`twitter`/`alternates.canonical` would surface the throwaway URL space into shared-link previews and the canonical-link metadata, both of which override the noindex intent for crawlers that ignore robots — OG/Twitter cards are still embedded by social-graph crawlers regardless of robots.txt), (ii) the `DownloadCta` deep-link URL contract — preferred-format-as-primary and `encodeURIComponent` defense on the raw input — currently entirely unpinned (a regression flipping primary to always-csv silently downgrades every shared-link's primary CTA; a regression dropping the encoder silently lets paste-input like `placeId="ChIJ&format=xlsx"` reach the `/api/reviews` route as two parsed pairs instead of one), and (iii) the `format` searchParam validation default (`isFormat(formatRaw) ? formatRaw : "csv"` — a refactor to `formatRaw ?? "csv"` drops validation and lets `format=garbage` flow through as the preferred format). Phase 16 walks that gap with one consolidated leaf. Same committed-not-run posture as Phase 11–15 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L16.1 Deepen `tests/preview-route.test.ts` — the result-preview server-component suite that already pins the metadata noindex contract (`metadata.robots === { index: false }`), the ≤PREVIEW_COUNT (5) rendered-row cap, the rating_count-not-walk-count total (D-041), and the four bad-input degrade-not-throw paths — with the three cross-cutting load-bearing concerns the existing 8-it/4-describe suite never reached, in the Phase 13/D-069/D-070/D-072 "public-export end-to-end" pattern (D-073). **(a) `metadata` exact-shape via `Object.keys(metadata).sort()` + `metadata.title` literal pin.** The existing test pins `metadata.robots` via `toEqual({ index: false })` but never (i) pins `metadata.title === "Review preview"` (the only other documented key — a refactor renaming the browser-tab title would land silently) and (ii) asserts `Object.keys(metadata).sort()` is exactly `["robots","title"]` (the surplus-keys threat: a refactor adding `metadata.openGraph` or `metadata.twitter` would surface the per-query throwaway URL into Twitter cards / Slack unfurls / Facebook OG previews — these are still embedded by social-graph crawlers that ignore robots.txt, leaking the throwaway URL space; a refactor adding `metadata.alternates: { canonical: "..." }` would point search engines at a canonical that overrides the noindex intent for crawlers honouring rel=canonical above robots). Symmetric structural-shape pin with L13.2/D-070's healthcheck `Object.keys(body)` pin and L15.1/D-072's api-reviews `Object.keys(body)` pin — same "shape of what we ship to the user is the contract" principle (D-051). **(b) `DownloadCta` deep-link URL contract — preferred-format-as-primary + `encodeURIComponent` defense — pinned end-to-end through `PreviewPage`.** The route builds `/api/reviews?placeId=${encodeURIComponent(placeIdInput)}&format=${fmt}` in `DownloadCta` and the existing suite never inspects these hrefs. Two regressions pinned: (i) **preferred-format primary** — `PreviewPage({searchParams: {placeId: "MOCK_SMALL_001", format: "xlsx"}})` produces a tree whose first `<a>` element with an `/api/reviews?` href contains `format=xlsx` (not `format=csv`); the secondary anchors contain the two remaining formats; the route's `ordered = [preferred, ...SUPPORTED_FORMATS.filter(...)]` puts the user-preferred format first, and a refactor flipping to "always csv first" or "preferred always last" would silently downgrade every shared-link's primary CTA without breaking any current assertion. (ii) **`encodeURIComponent` defense** — `PreviewPage({searchParams: {placeId: "ChIJ&malicious=true"}})` produces hrefs whose query string contains `placeId=ChIJ%26malicious%3Dtrue` (the `&` encoded as `%26`, the `=` as `%3D`), proving the encoder fires; a refactor dropping the encoder would emit `placeId=ChIJ&malicious=true` and the receiving `/api/reviews` route would parse `malicious=true` as a separate (silently-ignored) param while `placeId` was just `ChIJ` — the malformed-input case is exactly the paste-surface the form GET-navigates here with. Reachable via a structural anchor-walk that invokes function components (the same pattern as the existing `textOf` walk) and collects `<a>` elements (type-string equality `el.type === "a"`), reading `props.href` for the assertions — no new infrastructure beyond what the existing tree-walk helpers already provide. **(c) `format` searchParam validation default — invalid/missing `format` defaults to csv; valid format passes through; case is significant.** The route's `preferred: Format = isFormat(formatRaw) ? formatRaw : "csv"` is unguarded today (the existing tests never pass a `format` searchParam). Four sub-pins via the primary-anchor href: (i) no `format` searchParam → primary href contains `format=csv`; (ii) `format=garbage` → primary href still `format=csv` (validation, not pass-through — a refactor to `formatRaw ?? "csv"` drops the SUPPORTED_FORMATS gate and lets `format=garbage` reach `/api/reviews`, where the route would 400; this is a silent UX regression from "we picked a sensible default" to "you typed in a bad format and now the CTA is broken"); (iii) `format=xlsx` (valid) → primary href contains `format=xlsx` (pass-through works for valid values); (iv) `format=CSV` (upper-cased, valid Format value member-string would be lowercase) → primary href contains `format=csv` (case is significant in `isFormat`'s `.includes` check — symmetric with the `/api/reviews` route's case-insensitive `format` handling at L15.1; the preview's case-sensitive validation is the documented behaviour and the leaf pins it so a "harmonise to case-insensitive" cleanup is a loud change, not a silent one). Suite grows from 8 it-blocks/4 describes to ~17 it-blocks/7 describes (metadata-shape: 2 it / 1 new describe; CTA URL contract: 4 it / 1 new describe — preferred+encode+all-three-formats; format-validation: 4 it / 1 new describe — missing/garbage/valid/upper-case); suite-file count unchanged at 19 (deepening only, no new file). Two small structural-walk helpers added to the existing tree-walk machinery (`collectAnchors` and `firstApiReviewsHref`) — no new imports, no new dependencies, same `eachElement` traversal idiom. Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 17 — Suite-deepening tranche, review-tool-form a11y/paste-safety half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-073/D-074: with L16.1 merged (#70), every standard Phase 0–16 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). D-073's closing line named `tests/review-tool-form.test.ts` (pinned at the GET-vs-POST + action-target level) as the next candidate. The existing 7-it/4-describe suite pins the form action/method, the placeId input's name+`required`, the three format radios' name/order/defaultChecked, and the submit button — but never (i) the `<label htmlFor="placeId">` ↔ `<input id="placeId">` accessibility pairing (a refactor that renamed the id silently breaks screen-reader association), (ii) the paste-safety attributes `autoComplete="off"` + `spellCheck={false}` (a refactor dropping either lets mobile autofill/auto-correct silently corrupt a `ChIJ...` paste), and (iii) the `placeholder` documenting both accepted input forms (`maps.google.com` URL AND `ChIJ` short form per L1.5/D-018; a narrowed placeholder silently degrades UX for users pasting the other form). Same committed-not-run posture as Phase 11–16 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L17.1 Deepen `tests/review-tool-form.test.ts` — the form-shell suite for `app/_components/review-tool-form.tsx` that already pins the D-041 `<form action="/preview" method="GET">` rewire, the placeId input name+required, the three format radios (name/order/defaultChecked=json), and the lone submit button — with the three cross-cutting load-bearing concerns the existing 7-it/4-describe suite never reached, in the Phase 13/D-069/D-070/D-072/D-073 "pin the silent-regression edges of an already-tested module" pattern (D-074). **(a) Accessibility: `<label htmlFor="placeId">` ↔ `<input id="placeId">` pairing.** Screen readers walk `htmlFor` → `id` to associate the visible label with the input; a refactor that dropped `id="placeId"` from the input, renamed it to `placeIdInput`, or moved `htmlFor` to a different label silently breaks AT users (sighted form looks identical; AT users see an unlabelled text field). Pinned as two `it`s — exactly one `<label htmlFor="placeId">` exists, and the placeId text input carries the exact matching `id="placeId"` — so a regression on either side fails on its own assertion. **(b) Paste-safety attrs on the placeId input — `autoComplete="off"` + `spellCheck={false}` + `type="text"`.** Mobile browsers and modern desktop browsers offer autofill suggestions and underline pasted text as misspellings; either silently corrupts a `ChIJN1t_tDeuEmsRUsoyG83frY4` paste — autofill swaps it for a saved value, spellcheck "corrects" the `ChIJ` prefix to `Chij`/`CHIJ`. Both are silent visual UX bugs (the form still submits; the value is just wrong); no other test catches them. Pin `autoComplete="off"` (strict equality on the string `"off"`), `spellCheck === false` (the boolean prop value, surfacing a `spellCheck="false"` string-form drift the post-React-17 typings reject), and `type === "text"` (rejecting the three common slips — `email`, `url`, `search` — that enforce keyboard validation rejecting valid place_id chars OR add the UA reset-button affordance). **(c) `placeholder` documents every accepted input form — `maps.google.com` AND `ChIJ`.** Methodology §1 / L1.5 / D-018 documents three accepted input forms: Google Maps place URL, `place_id` (`ChIJ...`), and numeric CIDs; the placeholder is the only on-screen affordance that tells the user what to paste. A regression that narrowed it to URL-only (or ChIJ-only) is silent — the form still submits, the user hits a `bad_request` error card downstream, bounces. Pin (i) `maps.google.com` substring presence (case-insensitive — the URL form is robust to host-case drift) and (ii) `ChIJ` substring presence (case-significant — `ChIJ` is the canonical prefix the SF docs name per D-018; a placeholder of `chij...` would be user-confusing). Suite grows from 7 it-blocks/4 describes to 14 it-blocks/7 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 26 — Suite-deepening tranche, export-xlsx module-surface + per-call bytes-freshness half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-083: with L25.1 merged (#80), every standard Phase 0–25 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). D-082's closing line named `tests/export-xlsx.test.ts` as one of the next agent-doable deepening candidates (the XLSX writer's `Object.keys(module)` surface is unpinned even though L12.1/D-065 deepened the per-column/per-row semantics; the `__testing` namespace's key surface is also unpinned; the per-call freshness of the returned `Uint8Array` is not pinned, so a memoise-by-payload refactor could silently share a single buffer across every call and let downstream mutation leak across requests). Same committed-not-run posture as Phase 11–25 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L26.1 Deepen `tests/export-xlsx.test.ts` with three cross-cutting concerns the existing suite never reached, in the Phase 13/15/18/19/20/21/22/23/24/25/D-027/D-070/D-072/D-075/D-076/D-077/D-078/D-079/D-080/D-081/D-082 "pin the silent-regression edges of an already-tested module" pattern (D-083). **(a) Module's named-export surface.** `Object.keys(xlsxModule).sort()` exactly `["XLSX_COLUMNS","XLSX_CONTENT_TYPE","__testing","formatReviewsAsXlsx","xlsxFilename"]` via `import * as xlsxModule from "@/lib/export/xlsx"`; the type-only export `type XlsxColumn` is erased at runtime and does not appear. A surplus `export const SHEET_NAME` re-exposing the internal sheet name, an `export function buildWorkbook` helper extracted mid-refactor, or an `export const FREEZE_PANE` constant would silently broaden the public contract every downstream importer (the route handler, future SDK shims) is held to. Mirrors L18.1/D-075's variant-route + L20.1/D-077's faq-module + L21.1/D-078's home-route + L22.1/D-079's root-layout + L23.1/D-080's sf-client + L24.1/D-081's reviews-cache + L25.1/D-082's place-id exact-surface pins, applied to the XLSX writer module. **(b) `formatReviewsAsXlsx(payload)` returns a fresh `Uint8Array` per call.** Two `it`s — one for the non-empty `payload()` path, one for the empty-reviews path — so a "memoise by payload reference / content-hash" refactor (`const CACHE = new WeakMap<CachedReviewsPayload, Uint8Array>(); ... return cached;`) on either branch fails on its own assertion. The route handler reads `bytes.byteLength` for the Content-Length header and passes the same buffer to `NextResponse`; a shared singleton would mean two concurrent requests with the same payload reference race on the buffer, and any defensive trim/rewrite would leak across callers. Mirrors L19.1/D-076's per-Question + L20.1/D-077's per-call element-tree + L21.1/D-078's per-call HomePage tree + L22.1/D-079's per-call RootLayout tree + L23.1/D-080's per-call SF-client + L24.1/D-081's per-call cache-factory + L25.1/D-082's per-call `{raw, slug}` freshness pins, pushed onto the bytes the writer hands to the response. **(c) `__testing` namespace's exact key surface.** `Object.keys(__testing).sort()` exactly `["COLUMN_WIDTHS","PHOTO_URL_JOIN","SHEET_NAME","rowFor"]` — four helpers, all already consumed by this suite via the top-level destructure. A surplus 5th helper (e.g. a `freezePane` builder extracted mid-refactor, a `widthFor(col)` lookup, the unexported `Row` type re-exposed as a runtime sentinel) leaking in would silently broaden the test-only contract. Symmetric with L23.1/D-080's + L24.1/D-081's + L25.1/D-082's `__testing` exact-key-surface pins. Suite grows from 23 it-blocks/8 describes to 27 it-blocks/11 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 25 — Suite-deepening tranche, place-id module-surface + per-call freshness half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-082: with L24.1 merged (#79), every standard Phase 0–24 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). D-081's closing line named `tests/place-id.test.ts` as one of the next agent-doable deepening candidates (the canonicalisation module's `Object.keys(module)` surface is unpinned even though L11.3/D-061 deepened the per-rule semantics; the `__testing` namespace's key surface is also unpinned; the per-call freshness of the returned `{raw, slug}` object is not pinned, so a memoise-by-input refactor could silently share a single result object across every call and let downstream mutation leak across requests). Same committed-not-run posture as Phase 11–24 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L25.1 Deepen `tests/place-id.test.ts` with three cross-cutting concerns the existing suite never reached, in the Phase 13/15/18/19/20/21/22/23/24/D-027/D-070/D-072/D-075/D-076/D-077/D-078/D-079/D-080/D-081 "pin the silent-regression edges of an already-tested module" pattern (D-082). **(a) Module's named-export surface.** `Object.keys(placeIdModule).sort()` exactly `["PlaceIdParseError","__testing","normalisePlaceId"]` via `import * as placeIdModule from "@/lib/semanticforce/place-id"`; the type-only export `type NormalisedPlaceId` is erased at runtime and does not appear. A surplus `export const PLACE_ID_REGEX` / `export function isValidPlaceId` silently broadens the public contract every downstream importer (the route handler, the form validator, future SDK shims) is held to. Mirrors L18.1/D-075's variant-route + L20.1/D-077's faq-module + L21.1/D-078's home-route + L22.1/D-079's root-layout + L23.1/D-080's sf-client + L24.1/D-081's reviews-cache exact-surface pins, applied to the place-id canonicalisation module. **(b) `normalisePlaceId(input)` returns a fresh `{raw, slug}` object per call.** Two `it`s — one for the canonical ChIJ path, one for the MOCK_ family — so a "memoise by input" refactor (`const RESULTS = new Map<string, NormalisedPlaceId>(); ... return cached;`) on the MOCK branch passes the ChIJ assertion vacuously but fails its own. The cache layer hands the returned `{raw, slug}` around by reference; a shared singleton would let any downstream `.raw`/`.slug` mutation (a defensive `.toLowerCase()` on the slug, a UI layer holding onto the object and overwriting `.raw`) leak across requests with no `.toEqual()` test catching it. Mirrors L19.1/D-076's per-Question + L20.1/D-077's per-call element-tree + L21.1/D-078's per-call HomePage tree + L22.1/D-079's per-call RootLayout tree + L23.1/D-080's per-call SF-client + L24.1/D-081's per-call cache-factory freshness pins, pushed onto a pure normalisation function whose result object the cache layer references. **(c) `__testing` namespace's exact key surface.** `Object.keys(__testing).sort()` exactly `["PLACE_ID_PATTERN","canonicalisePrefix","slugify"]` — three helpers, all already consumed by this suite. A surplus 4th helper (e.g. `SHORT_LINK_HOSTS` re-export, an `isMockId` predicate, a `stripMapsUrl` helper extracted mid-refactor) leaking in would silently broaden the test-only contract. Symmetric with L23.1/D-080's + L24.1/D-081's `__testing` exact-key-surface pins. Suite grows from 19 it-blocks/6 describes to 23 it-blocks/9 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 24 — Suite-deepening tranche, reviews-cache module-surface + factory-freshness half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-081: with L23.1 merged (#78), every standard Phase 0–23 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). The existing `tests/reviews-cache.test.ts` (17 it / 5 describes) covers the versioned key shape, the `CACHE_TTL_SECONDS` absolute literal (L12.2), `MemoryCache` TTL boundary direction + injected clock, `KvRestCache` set/get pipeline body + Bearer auth + Content-Type symmetry, the four-way error-class fan-in on `get`, and the `set` best-effort swallow — but never (i) freezes the **`lib/cache/reviews-cache.ts` module's named-export surface** (a surplus `export const CACHE_VERSION` / `export function clearAll` would silently broaden the public contract every downstream importer is held to), (ii) pins that **`createReviewsCache()` returns a fresh cache instance per call** on either branch (a `const SINGLETON = new MemoryCache(...); return SINGLETON;` "DRY" refactor on the fixture branch would let MemoryCache's internal store leak across requests — same regression target on the KV branch), or (iii) pins that the `__testing` namespace's **key surface is exactly the two class-constructor handles** `MemoryCache` + `KvRestCache` — a surplus 3rd helper (e.g. `cacheKey` re-exported into the namespace, a `now` injection helper) leaking in would silently broaden the test-only contract. Same committed-not-run posture as Phase 11–23 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L24.1 Deepen `tests/reviews-cache.test.ts` with three cross-cutting concerns the existing suite never reached, in the Phase 13/15/18/19/20/21/22/23/D-027/D-070/D-072/D-075/D-076/D-077/D-078/D-079/D-080 "pin the silent-regression edges of an already-tested module" pattern (D-081). **(a) Module's named-export surface.** `Object.keys(reviewsCacheModule).sort()` exactly `["CACHE_KEY_PREFIX","CACHE_TTL_SECONDS","__testing","cacheKey","createReviewsCache"]` via `import * as reviewsCacheModule from "@/lib/cache/reviews-cache"`; type-only exports (`CachedReviewsPayload`/`ReviewsCache`/`ReviewsCacheOptions`) are erased at runtime and do not appear. A surplus export silently broadens the public contract every downstream importer is held to. Symmetric with L18.1/D-075's variant-route, L20.1/D-077's faq-module, L21.1/D-078's home-route, L22.1/D-079's root-layout, and L23.1/D-080's sf-client exact-surface pins, applied to the cache module. **(b) `createReviewsCache()` returns a fresh cache instance per call** on BOTH branches. Two `it`s — fixture/MemoryCache branch (two `createReviewsCache({now})` calls return reference-unequal MemoryCache instances) AND KV/KvRestCache branch (two `createReviewsCache({kvRestApiUrl, kvRestApiToken, fetchImpl})` calls return reference-unequal KvRestCache instances) — so a singleton hoist on either branch fails on its own assertion. The route handler runs per-request and assumes its cache is its own; a shared MemoryCache across all in-process requests would let a cache-set from one request silently serve a stale payload to a different placeId's cache-get. Mirrors L23.1/D-080's per-call factory-freshness pin pushed onto the cache factory. **(c) `__testing` namespace's exact key surface.** `Object.keys(__testing).sort()` exactly `["KvRestCache","MemoryCache"]` — two class-constructor handles, both already consumed by this suite via the top-level destructure. Symmetric with L23.1/D-080's `__testing` exact-key-surface pin, applied to the cache module's test-only escape-hatch namespace, so a surplus internal helper leaking into the namespace is a loud test failure rather than a silent broadening of the test-only contract. Suite grows by 4 it-blocks/3 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 23 — Suite-deepening tranche, sf-client module-surface + factory-freshness half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-080: with L22.1 merged (#77), every standard Phase 0–22 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). The existing `tests/sf-client.test.ts` (6 describes) covers the fixture-fallback path, the HTTP path (URL/headers, error mapping, network-throw wrapping), `__testing` helpers (clampLimit/pickFixture/cursor codec/mapStatusToCode), FixtureClient page-boundary slicing, HttpClient URL hygiene, and decodeCursor offset-field guards — but never (i) freezes the **`lib/semanticforce/client.ts` module's named-export surface** (a surplus export beyond `createSemanticForceClient`/`__testing` would silently broaden the public contract every downstream importer is held to), (ii) pins that **`createSemanticForceClient()` returns a fresh client instance per call** with the same env (a `const SINGLETON = new FixtureClient(...); return SINGLETON;` "DRY" refactor would let request-scoped mutation leak across callers — the route-handler-per-request invariant), or (iii) pins that the `__testing` namespace's **key surface is exactly the helpers plus the `FIXTURES` record** (`clampLimit`/`pickFixture`/`encodeCursor`/`decodeCursor`/`mapStatusToCode`/`FIXTURES`) — `FIXTURES` is consumed by `tests/fixtures-contract.test.ts:51`, so the pin locks the actual 6-key surface (not an aspirational helper-only one) and makes a surplus internal helper leaking into the namespace a loud test failure rather than a silent API broadening. Same committed-not-run posture as Phase 11–22 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L23.1 Deepen `tests/sf-client.test.ts` with three cross-cutting concerns the existing suite never reached, in the Phase 13/15/18/19/20/21/22/D-027/D-070/D-072/D-075/D-076/D-077/D-078/D-079 "pin the silent-regression edges of an already-tested module" pattern (D-080). **(a) Module's named-export surface.** `Object.keys(sfClientModule).sort()` exactly `["__testing","createSemanticForceClient"]` via `import * as sfClientModule from "@/lib/semanticforce/client"`; a surplus export silently broadens the public contract every downstream importer is held to. **(b) `createSemanticForceClient()` returns a fresh client per call** (same env, no shared mutable state). Two `it`s — fixture-path reference-inequality across two calls with `SF_API_KEY` unset, AND HTTP-path reference-inequality across two calls with `SF_API_KEY`+`SF_API_BASE` set — so a `const SINGLETON` hoist on either branch fails on its own assertion. Mirrors L19.1/D-076 per-Question + L20.1/D-077 per-call element-tree + L21.1/D-078 per-call HomePage tree + L22.1/D-079 per-call RootLayout tree freshness pins, pushed onto the SF client factory. **(c) `__testing` namespace's exact key surface.** `Object.keys(__testing).sort()` exactly `["FIXTURES","clampLimit","decodeCursor","encodeCursor","mapStatusToCode","pickFixture"]` — six keys, including the `FIXTURES` record consumed by `tests/fixtures-contract.test.ts:51` (removing `FIXTURES` would break that suite, so the pin locks the actual exported surface). Symmetric with the route-module export-surface pins (L18.1/D-075, L22.1/D-079) applied to the test-only escape-hatch namespace, so a surplus internal helper leaking into the namespace is a loud test failure rather than a silent broadening of the test-only contract. Suite grows by 4 it-blocks/3 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 22 — Suite-deepening tranche, root-layout module-surface + per-call freshness + html-as-root half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-078/D-079: with L21.1 merged (#75), every standard Phase 0–21 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). The existing `tests/root-layout.test.ts` (7 it / 3 describes) pins the env-gated Plausible analytics snippet (absent/present/data-domain-threaded), the document shell (`<html lang="en">`, `<body>` design-token className, children pass-through), and the layout-owned static metadata — but never (i) freezes the **`app/layout.tsx` module's named-export surface** (a surplus `export const dynamic`/`revalidate`/`runtime`/`fetchCache`/`viewport` would silently override the layout's SSG/ISR/runtime/viewport contract without any per-export behavioural test catching it, symmetric with L18.1/D-075 + L20.1/D-077 + L21.1/D-078 applied to the root layout module); (ii) pins that **`RootLayout()` returns a fresh element tree per call** with the same `children` prop (a `const TREE = (<html>...</html>); return TREE;` "DRY" refactor would freeze the tree at module-load and let any downstream reconciler/test mutation leak across renders — same idea as L21.1/D-078's `HomePage` freshness pin, pushed onto the root-layout's own returned tree, with the env-gated `<Script>` branch as a second freshness vector); or (iii) pins that **`<html>` is the *outermost* returned element**, not just *present* (the existing `html.length === 1` count passes whether `<html>` sits at the root of the returned tree or is wrapped in any `<Fragment>`/array — wrapping silently breaks the document-root contract React expects from a Next App-Router root layout and the "the layout IS `<html>`" idiom). Same committed-not-run posture as Phase 11–21 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L22.1 Deepen `tests/root-layout.test.ts` — the render-contract guard for `RootLayout()` in `app/layout.tsx` that already pins the env-gated Plausible snippet (absent/present/data-domain-threaded), the document shell (`<html lang="en">` + `<body>` design-token className + children pass-through), and the layout-owned static metadata — with the three cross-cutting load-bearing concerns the existing 7-it/3-describe suite never reached, in the Phase 13/15/18/19/20/21/D-027/D-070/D-072/D-075/D-076/D-077/D-078 "pin the silent-regression edges of an already-tested module" pattern (D-079). **(a) Module's named-export surface.** `Object.keys(rootLayoutModule).sort()` exactly `["default","metadata"]`. The existing import of `metadata` proves the named export exists; this also catches a surplus `export const dynamic`/`revalidate`/`runtime`/`fetchCache`/`viewport`, each of which silently changes the root layout's SSG/ISR/runtime/viewport contract without any per-export behavioural test catching it. Symmetric with L18.1/D-075's variant-route exact-surface pin, L20.1/D-077's faq-module exact-surface pin, and L21.1/D-078's home-route exact-surface pin, applied to the root layout module. **(b) `RootLayout()` returns a fresh element tree per call.** Mirrors L21.1/D-078's `HomePage` freshness pin pushed onto the root-layout's returned tree. A `const TREE = (<html>...</html>); export default function RootLayout(...) { return TREE; }` "avoid the allocation" refactor would freeze the tree at module-load and let any downstream reconciler/test/dev-tools mutation leak across renders; `.toEqual()` cannot catch this because both calls still match structurally. Pinned as two `it`s — root `<html>` reference-inequality across two calls with the same `children` value, AND the `<body>` element reference-inequality across two such calls — so a hoist of the whole tree fails on the first, and a hoist of just the `<body>` subtree fails on the second. **(c) `<html>` is the *outermost* returned element.** The existing `html.length === 1` count passes whether `<html>` sits at the root of the returned tree or is wrapped in any outer container — a refactor that wrapped `<html>` in a `<Fragment>` or array would silently weaken the document-root contract (the App-Router root layout MUST return the `<html>` element as the root of the document tree React feeds to the runtime; wrapping breaks SSR + hydration). Pin `(tree as El).type === "html"` so the returned root must literally be the `<html>` element node. Suite grows from 7 it-blocks/3 describes to 12 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 21 — Suite-deepening tranche, home-page module-surface + composition-freshness + main-as-root half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-077/D-078: with L20.1 merged (#74), every standard Phase 0–20 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). The existing `tests/home-page.test.ts` (7 it / 3 describes) pins the shared-component composition (exactly one `<ReviewToolForm/>` + one `<FaqSection/>` + one `<main>` landmark), the absent page-level `metadata` (D-040 inherited-layout-metadata), and the D-041 placeholder-stays-gone deep flatten — but never (i) freezes the **`app/page.tsx` module's named-export surface** (the existing `"metadata" in homePageModule` check catches a `metadata` shadow but not a surplus `export const dynamic`/`revalidate`/`runtime`/`fetchCache` — these silently affect SSG/ISR on the home page without any per-export behavioural test catching it, symmetric with L18.1/D-075 applied to the home route); (ii) pins that **`HomePage()` returns a fresh element tree per call** (a `const TREE = (...); return TREE;` "DRY" refactor would freeze the tree at module-load and let downstream React reconciler/test mutation leak across renders — same idea as L20.1/D-077's `FaqSection` freshness pins, pushed onto the home page's own returned tree); or (iii) pins that **the `<main>` is the *outermost* returned element**, not just *present* (the existing `mains.length === 1` count passes whether `<main>` sits at the root or is wrapped in any `<div>`/`<Fragment>`/other landmark — wrapping it silently weakens the AT-walk-to-outermost-landmark contract and the "the page IS the main" Next 15 App-Router idiom). Same committed-not-run posture as Phase 11–20 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L21.1 Deepen `tests/home-page.test.ts` — the render-contract guard for `HomePage()` in `app/page.tsx` that already pins the shared-component composition (exactly one `<ReviewToolForm/>` + one `<FaqSection/>` + one `<main>` landmark), the no-page-level-`metadata` shadow (D-040 inherited-layout-metadata), and the D-041 placeholder-stays-gone deep flatten — with the three cross-cutting load-bearing concerns the existing 7-it/3-describe suite never reached, in the Phase 13/15/18/19/20/D-027/D-070/D-072/D-075/D-076/D-077 "pin the silent-regression edges of an already-tested module" pattern (D-078). **(a) Module's named-export surface.** `Object.keys(homePageModule).sort()` exactly `["default"]`. The existing `"metadata" in homePageModule` check catches a `metadata` shadow but not a surplus `export const dynamic = "force-dynamic"` / `revalidate = N` / `runtime = "edge"` / `fetchCache = "..."` — these each silently affect the home page's SSG/ISR/runtime contract without any per-export behavioural test catching them (the page still renders correctly; only its build-time/serve-time posture changes). Symmetric with L18.1/D-075's variant-route exact-surface pin and L20.1/D-077's faq-module exact-surface pin, applied to the home route module. **(b) `HomePage()` returns a fresh element tree per call.** Mirrors L20.1/D-077's `FaqSection` and L19.1/D-076's per-Question freshness pin pushed onto the home page's own returned tree. A `const TREE = (<main>...</main>); export default function HomePage() { return TREE; }` "avoid the allocation" refactor would freeze the tree at module-load and let any downstream reconciler/test/dev-tools mutation leak across renders; `.toEqual()` cannot catch this because both calls still match structurally. Pinned as two `it`s — root `<main>` reference-inequality across two calls, AND the nth direct child of `<main>` reference-inequality — so a hoist of the whole tree fails on the first, and a hoist of just the inner children array fails on the second. **(c) `<main>` is the *outermost* returned element.** The existing `mains.length === 1` count passes whether `<main>` sits at the root of the returned tree or is wrapped in any outer container — a refactor that wrapped `<main>` in a `<div>` or `<Fragment>` (or moved `<main>` into a child) would silently weaken the landmark semantics (AT walks the page for the *outermost* landmark first; nesting `<main>` deeper makes the page-as-main contract a lie) and break the Next 15 App-Router "the page IS the main" idiom. Pin `(tree as El).type === "main"` so the returned root must literally be the `<main>` element node. Suite grows from 7 it-blocks/3 describes to 12 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 20 — Suite-deepening tranche, faq-section module-surface + per-call freshness + body-shell half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-076/D-077: with L19.1 merged (#73), every standard Phase 0–19 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). The existing `tests/faq-section.test.ts` (7 it / 3 describes) pins the `<section aria-labelledby="faq-heading">` + `<h2 id="faq-heading">FAQ</h2>` accessible pair, the nth-`<summary>`-matches-`FAQ_ITEMS[i].q` order, the no-stray-`open` default-closed contract, the rich-JSX `<code>` probe on the rate-limits item, and the entity-decoding guard — but never (i) freezes the **`faq.tsx` module's named-export surface** (a surplus `export const FAQ_HEADING` would silently broaden the public contract); (ii) pins that **`FaqSection()` returns a fresh element tree per call** (a `const TREE = (...); return TREE;` "DRY" refactor would freeze the tree at module-load and let downstream mutation leak across renders — same idea as L18.1/L19.1's freshness pins but at the React element-tree level instead of `Metadata` / `FAQPage`-payload level); or (iii) pins the **per-`<details>` body shell** — exactly one `<summary>` and exactly one `<p>` body container per entry (a refactor that dropped the `<p>` wrapper would silently lose the `mt-3 text-sm text-muted-foreground` styling on every answer). Same committed-not-run posture as Phase 11–19 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L20.1 Deepen `tests/faq-section.test.ts` — the render-contract guard for `FaqSection()` in `app/_components/faq.tsx` that already pins the region shell + accessible labelling, the per-item `<details>`/`<summary>` order against `FAQ_ITEMS`, the default-closed accordion contract, and the rich-JSX-vs-plain-text divergence-by-design — with the three cross-cutting load-bearing concerns the existing 7-it/3-describe suite never reached, in the Phase 13/15/18/19/D-027/D-070/D-072/D-075/D-076 "pin the silent-regression edges of an already-tested module" pattern (D-077). **(a) Module's named-export surface.** `Object.keys(faqModule).sort()` exactly `["FAQ_ITEMS","FaqSection","faqJsonLd"]`. The two downstream callsites (`app/page.tsx` for the home page and `app/(seo)/[variant]/page.tsx` for the inert-until-L3.1b variant route) import only those three names; a surplus export silently broadens the public contract every consumer is held to. Symmetric with L18.1/D-075's `routeModule` exact-surface pin applied to a component module instead of a Next route module. **(b) `FaqSection()` returns a fresh element tree per call.** Mirrors L18.1/D-075's `generateMetadata` and L19.1/D-076's per-Question/per-Answer freshness pin pushed one layer up — onto the React element-tree level. A `const TREE = (<section>...</section>); export function FaqSection() { return TREE; }` "avoid the allocation" refactor would freeze the tree at module-load and let any downstream reconciler/test/dev-tools mutation leak across renders; `.toEqual()` cannot catch this because both calls still match structurally. Pinned as two `it`s — root `<section>` reference-inequality, AND nth `<details>` reference-inequality across two calls — so a regression at either layer fails on its own assertion (a hoist of just the inner `.map(...)` would shipthe roots fresh but the children shared, and the second `it` is the one that catches it). **(c) Per-`<details>` body shell — exactly one `<summary>` and exactly one `<p>` per entry.** The existing rich-JSX describe proves item.a's *contents* reach the page (via the `<code>` probe on the rate-limits item); it does not pin the *shell*. A refactor that dropped the `<p>` body container would silently lose the `mt-3 text-sm text-muted-foreground` styling on every answer; a refactor that emitted a second `<summary>` (or zero) would break the accordion's click-to-toggle contract on every entry, again silently from any current suite's POV. Suite grows from 7 it-blocks/3 describes to 12 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 19 — Suite-deepening tranche, faq-jsonld object-keys-surface + per-Question freshness half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-075/D-076: with L18.1 merged (#72), every standard Phase 0–18 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). The existing `tests/faq-jsonld.test.ts` (9 it / 2 describes) proves shape (@context/@type/mainEntity) and parity (acceptedAnswer.text == item.text) but never freezes the exact set of keys at each level of the JSON-LD object — a refactor adding surplus fields (e.g. `url`, `inLanguage`, `dateModified`, `publisher`) at any level would pass every existing assertion but ship surplus structured data Google may flag as "incorrect type" rich-result warnings, or fingerprint the page differently across builds. Same committed-not-run posture as Phase 11–18 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L19.1 Deepen `tests/faq-jsonld.test.ts` — the FAQPage JSON-LD suite that already pins schema.org shape + acceptedAnswer↔item.text parity + JSON round-trip + top-level freshness — with the two cross-cutting load-bearing concerns the existing suite never reached, in the Phase 13/15/18/D-027/D-070/D-072/D-075 "freeze the exact object-keys surface" pattern (D-076). **(a) Exact object-keys surface at all three nesting levels.** `Object.keys(ld).sort()` exactly `["@context","@type","mainEntity"]`; `Object.keys(entity).sort()` exactly `["@type","acceptedAnswer","name"]` for every Question; `Object.keys(answer).sort()` exactly `["@type","text"]` for every acceptedAnswer. A surplus field at any level (e.g. `url` on the Answer, `image` on the Question, `dateModified` at the top) ships invalid/over-extended JSON-LD that Google's Rich Results tester can flag. Symmetric with the response-envelope `Object.keys(body).sort()` pattern (L13.1/L13.2/L15.1) but applied to a structured-data payload instead of an HTTP body. **(b) Per-Question + per-Answer freshness across calls.** The existing suite only proves the top-level object is reference-fresh per call; a refactor that hoisted `const QUESTIONS = FAQ_ITEMS.map(...)` to module scope would pass `.toEqual()` (still value-equal) but share mutable Question objects across every call — a single downstream mutation (`q.name = "..."` or `q.acceptedAnswer.text = "..."`) would leak into every subsequent response. Asserted with `entity !== b.mainEntity[i]` AND `entity.acceptedAnswer !== b.mainEntity[i].acceptedAnswer` so a regression at either nesting level fails on its own assertion. Mirrors L18.1/D-075's `generateMetadata`/`generateStaticParams` "fresh object/array per call" idea pushed one level deeper into nested objects. Suite grows from 9 it-blocks/2 describes to 13 it-blocks/4 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Phase 18 — Suite-deepening tranche, variant-route module-surface + freshness half (agent-doable, no creds/deploys)

_Opened per D-046/D-051/D-074/D-075: with L17.1 merged (#71), every standard Phase 0–17 leaf is `[x]` except the five corgi/human-gated ones (L1.6b, L3.1b, L4.1, L5.1, L5.2). D-074's closing line named `tests/variant-page.test.ts` as the next candidate. The existing 9-it/4-describe suite pins `dynamicParams === false`, `generateStaticParams()` enumerates `publishedVariants()`, `generateMetadata` returns `{}` for both unpublished and garbage slugs, and `VariantPage` calls `notFound()` for every current slug (with a render-path zero-count belt). It never (i) pins the **module's named-export surface** (a surplus `export const dynamic`/`revalidate`/`runtime`/`fetchCache` would silently override the `dynamicParams = false` inert-posture or flip the SSG/ISR contract without any current test catching it), (ii) pins that **`generateMetadata` returns a fresh `{}` per call** (a `const EMPTY: Metadata = {}; return EMPTY;` "avoid the allocation" refactor would let Next or any downstream caller mutate the shared instance and leak across requests — `.toEqual({})` cannot catch this because both calls still match the empty-shape literal), or (iii) pins that **`generateStaticParams` builds a fresh array per call** (a `const PARAMS = publishedVariants().map(...); return PARAMS;` "DRY" refactor caches at module-load and freezes the SSG-params list — when L3.1b lands or the registry mutates in dev/HMR, the stale module-level constant would be served forever until restart). Same committed-not-run posture as Phase 11–17 (D-039/D-040/D-042: manifest-only, no `node_modules`, runs on `npm install && npm test`)._

- [x] L18.1 Deepen `tests/variant-page.test.ts` — the inert-until-L3.1b suite for `app/(seo)/[variant]/page.tsx` that already pins `dynamicParams === false`, `generateStaticParams()` enumerates `publishedVariants()` (empty pre-L3.1b), `generateMetadata` returns `{}` for both unpublished and garbage slugs, and `VariantPage` calls `notFound()` for every current registry slug — with the three cross-cutting load-bearing concerns the existing 9-it/4-describe suite never reached, in the Phase 13/D-069/D-070/D-072/D-073/D-074 "pin the silent-regression edges of an already-tested module" pattern (D-075). **(a) Module export surface — no surplus Next route-config exports.** The route's contract with Next 15 is exactly four named exports: `dynamicParams`, `generateStaticParams`, `generateMetadata`, and `default` (VariantPage). A refactor adding `export const dynamic = "force-dynamic"` silently overrides `dynamicParams = false` and turns unknown slugs into on-demand SSR renders (different 404 codepath, inert-until-L3.1b guarantee weakened to "if the published filter holds"); `export const revalidate = N` flips SSG → ISR (refreshes on a clock); `export const runtime = "edge"` moves the route off Node (different cold-start, different fetch semantics). None of the existing per-export behavioural tests catch these because they each still behave as pinned — only the *surface* of the module changes. Pinned via `Object.keys(routeModule).sort()` exact-array equality to `["default","dynamicParams","generateMetadata","generateStaticParams"]`, symmetric with the L13.1/L13.2/L15.1 envelope-shape `Object.keys(body).sort()` pattern (D-027/D-070/D-072) applied to the module's export surface instead of a response body. **(b) `generateMetadata` returns a fresh `{}` per call (no shared mutable state).** The current `return {};` builds a fresh literal each call; a "avoid the allocation" refactor to `const EMPTY: Metadata = {}; ... return EMPTY;` would let Next or any downstream caller mutate the shared instance — a single `meta.title = "..."` write would leak into every subsequent call's response. `.toEqual({})` cannot catch this because both calls still match the empty-shape literal; reference inequality (`a !== b`) is the strict-stronger pin. Two `it`s — one for the unpublished registry slug, one for the garbage slug — so a regression on either branch of `findPublishedVariant` (registry hit + published filter fail vs. no registry hit at all) fails on exactly that input. Mirrors L11.1/D-064's `faqJsonLd()` "is a fresh object each call" and L14.1/D-071's sitemap "two `sitemap()` calls return reference-unequal Date objects" precedent. **(c) `generateStaticParams` returns a fresh array per call.** The current `publishedVariants().map(...)` allocates new arrays (filter + map); a "DRY" refactor to `const PARAMS = publishedVariants().map(...); export function generateStaticParams() { return PARAMS; }` would freeze the SSG-params list at module-load — when L3.1b lands or the registry mutates in dev/HMR, the stale constant would serve forever until restart, and the L3.1b flip's "static-params follow `publishedVariants()`" contract would silently break in dev workflows. Two calls return `[]` pre-L3.1b (value-equal); reference inequality (`a !== b`) proves the function builds fresh. Suite grows from 9 it-blocks/4 describes to 13 it-blocks/6 describes; suite-file count unchanged at 19 (deepening only, no new file). Same committed-not-run posture (D-039/D-040/D-042).

---

## Out-of-scope parking lot

- Multi-place batch export
- Sentiment analysis / summarisation
- Browser extension
- Competitor monitoring
- White-label embed
